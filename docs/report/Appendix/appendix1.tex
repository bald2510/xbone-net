\appendix

\chapter{Chi tiết về bộ dữ liệu}
\label{Appendix1}

Phụ lục này bổ sung các thông tin kỹ thuật cần thiết để tái lập quá trình chuẩn bị hai bộ dữ liệu BTXRD và CTCH. Nội dung của mỗi bộ dữ liệu được tổ chức theo cấu trúc dữ liệu, cách xây dựng nguồn văn bản sử dụng trong thực nghiệm đối với bộ dữ liệu BTXRD và cách sàng lọc dữ liệu đối với bộ dữ liệu CTCH. Các thống kê được tính trực tiếp từ phiên bản dữ liệu mà mã nguồn hiện tại sử dụng.

\section{Bộ dữ liệu BTXRD}
\label{AppendixBTXRD}

Phần này trình bày cấu trúc dữ liệu, đặc điểm kỹ thuật của ảnh và phương pháp xây dựng văn bản dạng lâm sàng tổng hợp cho BTXRD. Ảnh và siêu dữ liệu thuộc phiên bản BTXRD được sử dụng trong nghiên cứu riêng văn bản tổng hợp được sinh ngoại tuyến và cố định trước khi huấn luyện. Nội dung này tập trung vào cách sinh nguồn dữ liệu giả lâm sàng.

\subsection{Cấu trúc lưu trữ và đặc điểm kỹ thuật}

Bảng~\ref{tab:appendix-btxrd-storage} mô tả các thành phần liên quan đến phạm vi phân loại của nghiên cứu. Định danh ảnh là khóa liên kết một-một giữa siêu dữ liệu, nhãn, tập chia, ảnh và văn bản tổng hợp.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{|l|l|l|}
\hline
\textbf{Thành phần}                    & \textbf{Số tệp} & \textbf{Vai trò}                          \\ \hline
\texttt{images/}          & 3.746           & Lưu ảnh X-quang đầu vào.                  \\ \hline
\texttt{Annotations/} &
  1.867 &
  \begin{tabular}[c]{@{}l@{}}Lưu chú thích JSON cho các ảnh có u; \\ không được sử dụng trong huấn luyện phân loại.\end{tabular} \\ \hline
\texttt{reports/} &
  3.746 &
  \begin{tabular}[c]{@{}l@{}}Mỗi ảnh có một văn bản dạng lâm sàng được \\ sinh ngoại tuyến từ siêu dữ liệu.\end{tabular} \\ \hline
\texttt{dataset.csv} &
  3.746 &
  \begin{tabular}[c]{@{}l@{}}Siêu dữ liệu gốc, gồm định danh ảnh,\\  nguồn thu thập, tuổi, giới tính, vị trí giải phẫu, \\ tư thế chụp và các cờ nhóm bệnh.\end{tabular} \\ \hline
\texttt{btxrd-labels.csv} & 3.746           & Chứa định danh ảnh và 10 cột chỉ báo lớp. \\ \hline
\texttt{btxrd-split.csv} &
  3.746 &
  \begin{tabular}[c]{@{}l@{}}Ánh xạ mỗi ảnh vào \texttt{train}, \\ \texttt{validate} hoặc \texttt{test}.\end{tabular} \\ \hline
\end{tabular}%
}
\caption{Cấu trúc lưu trữ của bộ dữ liệu BTXRD trong nghiên cứu}
\label{tab:appendix-btxrd-storage}
\end{table}

Bảng~\ref{tab:appendix-btxrd-image-properties} cho thấy dữ liệu ảnh có định dạng không hoàn toàn đồng nhất. Mặc dù tất cả tên tệp đều mang định dạng JPEG, kiểm tra nội dung tệp xác định 3.744 ảnh được mã hóa JPEG, một ảnh được mã hóa BMP và một ảnh được nhận diện là MPO; toàn bộ 3.746 ảnh vẫn giải mã thành công. Khi nạp dữ liệu, mỗi ảnh được chuyển sang RGB trước khi tạo tensor, do đó ảnh thang xám và ảnh ba kênh đều tạo đầu vào ba kênh cho mô hình.

\begin{table}[H]
\centering
\caption{Đặc điểm kỹ thuật của các tệp ảnh BTXRD}
\label{tab:appendix-btxrd-image-properties}
\small
\resizebox{\textwidth}{!}{%
\begin{tabularx}{\textwidth}{|L{4.2cm}|X|}
\hline
\textbf{Thuộc tính} & \textbf{Kết quả kiểm tra} \\ \hline
Phần mở rộng & 3.719 tệp \texttt{.jpeg} và 27 tệp \texttt{.jpg}. \\ \hline
Chế độ màu khi giải mã & 2.895 ảnh thang xám \texttt{L} và 851 ảnh \texttt{RGB}. \\ \hline
Định dạng mã hóa & 3.744 JPEG, 1 BMP và 1 MPO. \\ \hline
Chiều rộng & Nhỏ nhất 153 px, trung vị 1.612 px, lớn nhất 3.594 px. \\ \hline
Chiều cao & Nhỏ nhất 311 px, trung vị 2.256 px, lớn nhất 4.881 px. \\ \hline
Khả năng giải mã & 3.746/3.746 tệp được đọc thành công. \\ \hline
\end{tabularx}%
}
\end{table}

\subsection{Phương pháp tạo dữ liệu giả lâm sàng}

BTXRD không cung cấp bệnh sử tự do tương ứng với từng ảnh. Để mô phỏng đầu vào văn bản của một hệ thống đa phương thức, nghiên cứu xây dựng một văn bản dạng lâm sàng cho mỗi mẫu bằng quy trình dựa trên luật và ngân hàng mẫu câu. Mục tiêu của quy trình là tạo một cấu trúc đầu vào thống nhất, không phải khôi phục bệnh sử thật của người bệnh.

\subsubsection{Thông tin đầu vào và luật điều kiện}

Các trường được lấy trực tiếp từ siêu dữ liệu gồm tuổi, giới tính, vị trí xương, vị trí khớp, vùng giải phẫu và tư thế chụp. Vị trí chính được chọn theo thứ tự ưu tiên xương, khớp, vùng giải phẫu nếu không có trường vị trí cụ thể, một mô tả tổng quát về xương sẽ được sử dụng. Tuổi được chia thành bốn nhóm gồm dưới 20 tuổi, 20-34 tuổi, 35-49 tuổi và từ 50 tuổi trở lên để lựa chọn ngữ cảnh tiền sử phù hợp.

Ngân hàng mẫu câu được chia thành ba nhóm hành vi. Mẫu không có cờ u được gán nhóm "không u" còn mẫu có cờ u và cờ ác tính được gán nhóm "ác tính" và các mẫu có u còn lại được gán nhóm "lành tính". Theo luật này, dữ liệu gồm 1.879 mẫu không u, 1.525 mẫu có u nhưng không mang cờ ác tính và 342 mẫu ác tính. Nhóm này quyết định tập mẫu câu dùng cho lý do vào viện, diễn tiến bệnh và khám thực thể. Ngoài ra, 182 mẫu có nhiều hơn một xương được đánh dấu và 61 mẫu có khớp liên quan thì được nối thêm một câu mô tả bối cảnh nhiều vị trí hoặc hạn chế vận động quanh khớp.

\subsubsection{Ghép văn bản và cố định dữ liệu}

Các lựa chọn ngẫu nhiên sử dụng phân phối đều trên từng ngân hàng câu và hạt giống 42. Ngân hàng mẫu câu ban đầu được sinh từ tên bệnh bằng ChatGPT-4o \cite{OpenAI2024GPT4oSystemCard}, tham chiếu cấu trúc các trường bệnh sử của CTCH để mô phỏng cách trình bày. Danh sách khoảng thời gian có 10 lựa chọn; số mẫu câu lý do vào viện của ba nhóm không u/lành tính/ác tính lần lượt là 6/7/6, số mẫu diễn tiến bệnh là 6/8/8 và số mẫu khám thực thể là 4/7/7. Ngân hàng tiền sử chung có 9 câu, mỗi nhóm tuổi có 3 câu ngữ cảnh, còn hai ngân hàng bổ sung nhiều xương và quanh khớp đều có 5 câu. Sau bước biên soạn ngân hàng, quy trình chọn và ghép văn bản không gọi mô hình ngôn ngữ và không có bước xác nhận nội dung bởi bác sĩ.

Với thứ tự hàng siêu dữ liệu được giữ cố định, mỗi lần chạy quy trình tạo ra cùng một lựa chọn mẫu câu. Văn bản được ghép theo thứ tự: thông tin người bệnh, lý do vào viện, diễn tiến bệnh, khám thực thể, tiền sử bản thân và thông tin thu nhận ảnh.

Bảng~\ref{tab:appendix-btxrd-template-examples} liệt kê các mẫu câu đại diện cho từng ngân hàng. Các mẫu được giữ nguyên bằng tiếng Anh vì đây là ngôn ngữ của đầu vào văn bản BTXRD trong thí nghiệm với các ký hiệu "location", "duration" và "joint" lần lượt được thay bằng vị trí chính, khoảng thời gian được chọn và khớp liên quan.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{|l|l|}
\hline
\textbf{Ngân hàng} &
  \textbf{Mẫu câu đại diện} \\ \hline
Khoảng thời gian &
  \begin{tabular}[c]{@{}l@{}}\textit{2 weeks}; \textit{1 month}; \textit{3 months}; \textit{6 months}; \\ \textit{several weeks}; \textit{a few months}; \textit{2 months}; \\ \textit{4 weeks}; \textit{several months}; \textit{1 year}.\end{tabular} \\ \hline
Lý do vào viện &
  \begin{tabular}[c]{@{}l@{}}Không u: \textit{Pain in the \{location\}}. \\ Lành tính: \textit{Swelling of the \{location\}}. \\ Ác tính: \textit{Persistent pain in the \{location\}}.\end{tabular} \\ \hline
Diễn tiến bệnh &
  \begin{tabular}[c]{@{}l@{}}Không u: \textit{Onset \{duration\} ago; mild symptoms}. \\ Lành tính: \textit{The patient noticed swelling in the \{location\} \{duration\} ago}. \\ Ác tính: \textit{Pain and swelling in the \{location\} for \{duration\}; progressively worsening}.\end{tabular} \\ \hline
Khám thực thể &
  \begin{tabular}[c]{@{}l@{}}Không u: \textit{No tenderness or swelling on palpation}. \\ Lành tính: \textit{Palpable firm mass; non-tender; no skin changes}. \\ Ác tính: \textit{Firm, tender mass on the \{location\}; local warmth noted}.\end{tabular} \\ \hline
Bổ sung nhiều xương &
  \begin{tabular}[c]{@{}l@{}}\textit{Multiple bony prominences palpated at several sites}; \\ \textit{Multiple hard masses involving several bones on examination}.\end{tabular} \\ \hline
Bổ sung quanh khớp &
  \begin{tabular}[c]{@{}l@{}}\textit{Joint involvement noted; restricted range of motion at the \{joint\}}; \\ \textit{Soft tissue swelling observed around the \{joint\}}.\end{tabular} \\ \hline
Tiền sử chung &
  \begin{tabular}[c]{@{}l@{}}\textit{No significant medical history}; \\ \textit{No prior bone disease or fracture}; \\ \textit{No relevant prior medical or surgical history}.\end{tabular} \\ \hline
Ngữ cảnh theo tuổi &
  \begin{tabular}[c]{@{}l@{}}Dưới 20 tuổi: \textit{Growth and development appropriate for age}. \\ Từ 20--34 tuổi: \textit{No prior bone lesions}. \\ Từ 35--49 tuổi: \textit{No relevant surgical or medical history}. \\ Từ 50 tuổi trở lên: \textit{History of age-related degenerative changes; no prior bone lesions}.\end{tabular} \\ \hline
\end{tabular}%
}
\caption{Các mẫu câu đại diện trong ngân hàng văn bản giả lâm sàng BTXRD}
\label{tab:appendix-btxrd-template-examples}
\end{table}

Quy trình không chèn trực tiếp tên của 10 lớp đích vào văn bản. Tuy nhiên, cờ có u và cờ ác tính tham gia trực tiếp vào việc chọn ngân hàng mẫu câu; vì vậy, nội dung tổng hợp vẫn mang tín hiệu liên hệ mạnh với nhãn. Các triệu chứng, thời gian mắc bệnh, dấu hiệu khám và tiền sử được chọn từ mẫu câu là dữ liệu giả lập, không phải lời khai của người bệnh hoặc ghi nhận của nhân viên y tế. Do đó, kết quả liên quan đến văn bản BTXRD chỉ phản ánh thiết lập giả lâm sàng có điều kiện này và không được diễn giải tương đương với bệnh sử tự nhiên của CTCH.

\subsubsection{Kiểm chứng liên kết và khả năng tái lập}

Để kiểm tra văn bản đã được ghép đúng với hàng siêu dữ liệu, bộ sinh được phát lại từ đầu trên toàn bộ \texttt{dataset.csv} với hạt giống 42 và giữ nguyên thứ tự 3.746 hàng. Văn bản tái tạo của mỗi hàng được so sánh từng ký tự với tệp có cùng định danh ảnh trong \texttt{reports/}. Cách kiểm tra tuần tự này là cần thiết vì lựa chọn mẫu câu của một hàng phụ thuộc vào trạng thái bộ sinh giả ngẫu nhiên sau các hàng đứng trước; gọi hàm sinh riêng lẻ cho một mẫu không bảo toàn trạng thái đó. Ngoài liên kết văn bản, phép kiểm tra còn đối chiếu nhãn với siêu dữ liệu nguồn và xác nhận quy mô ba tập chia. Kết quả được trình bày trong Bảng~\ref{tab:appendix-btxrd-text-verification}.

\begin{table}[H]
\centering
\small
\begin{tabular}{|L{8.2cm}|r|}
\hline
\textbf{Phép kiểm tra} & \textbf{Kết quả} \\ \hline
Định danh duy nhất trong siêu dữ liệu, nhãn và tập chia & 3.746/3.746 (100\%) \\ \hline
Nhãn có đúng một lớp trong 10 lớp & 3.746/3.746 (100\%) \\ \hline
Nhãn khớp với các cờ lớp trong \texttt{dataset.csv} & 3.746/3.746 (100\%) \\ \hline
Quy mô \texttt{train}/\texttt{validate}/\texttt{test} & 2.621/375/750 \\ \hline
Hàng siêu dữ liệu có văn bản cùng định danh & 3.746/3.746 (100\%) \\ \hline
Văn bản khớp chính xác khi phát lại bộ sinh & 3.746/3.746 (100\%) \\ \hline
Tệp thiếu/tệp dư so với \texttt{dataset.csv} & 0/0 \\ \hline
Văn bản có đủ sáu trường cấu trúc & 3.746/3.746 (100\%) \\ \hline
Văn bản duy nhất & 3.745/3.746 (99,97\%) \\ \hline
Độ dài trung bình & $57{,}70 \pm 6{,}66$ từ \\ \hline
Trung vị và miền độ dài & 56 từ; 44--91 từ \\ \hline
Thiếu tuổi, giới tính, vị trí hoặc tư thế chụp & 0 mẫu \\ \hline
Xuất hiện trực tiếp tên bệnh đích trong danh sách kiểm tra & 0/3.746 \\ \hline
\end{tabular}
\caption{Kết quả kiểm chứng tính toàn vẹn của dữ liệu BTXRD}
\label{tab:appendix-btxrd-text-verification}
\end{table}

Hai mẫu \texttt{IMG002607.jpeg} và \texttt{IMG003588.jpeg} tạo cùng một văn bản, giải thích vì sao số văn bản duy nhất thấp hơn tổng số mẫu một đơn vị. Kiểm tra từ khóa không phát hiện sự xuất hiện trực tiếp của các tên bệnh đích như \textit{osteochondroma}, \textit{osteosarcoma}, \textit{simple bone cyst} hoặc \textit{giant cell tumor}. Phép kiểm tra này xác nhận bộ sinh không chèn trực tiếp các tên bệnh được rà soát, nhưng không tự loại trừ tín hiệu nhãn gián tiếp. Trong phiên bản dữ liệu hiện tại, cả 3.746 hàng đều là nhãn một-nóng; mẫu \texttt{IMG001276.jpeg} chỉ mang nhãn cụ thể \textit{synovial osteochondroma} và không còn đồng thời mang nhãn cha \textit{osteochondroma}.

\subsubsection{Kiểm tra rò rỉ nhãn trực tiếp và gián tiếp}

Quy trình được thực hiện trên toàn bộ 3.746 cặp văn bản--nhãn theo các bước sau:

\begin{enumerate}
    \item \textbf{Kiểm tra trực tiếp:} chuẩn hóa chữ hoa--chữ thường và rà soát mỗi báo cáo để tìm sự xuất hiện nguyên văn của tên bệnh đích cùng các biến thể tên đã định nghĩa trước.
    \item \textbf{Tạo câu nhắc lớp:} tạo một câu nhắc theo cùng mẫu \textit{Clinical history associated with [disease label]} cho từng lớp trong 10 lớp bệnh.
    \item \textbf{Mã hóa ngữ nghĩa:} mã hóa báo cáo và các câu nhắc lớp bằng bộ mã hóa văn bản BioMedCLIP, sau đó chuẩn hóa embedding và tính độ tương đồng cosin giữa mỗi báo cáo với cả 10 câu nhắc.
    \item \textbf{Tính cosine margin:} với mẫu $i$ có nhãn đúng $y_i$, tính
    \begin{equation}
        m_i = \cos(r_i,p_{y_i}) - \max_{c\ne y_i}\cos(r_i,p_c),
    \end{equation}
    trong đó $r_i$ là embedding của báo cáo và $p_c$ là embedding câu nhắc lớp $c$. Margin dương nghĩa là câu nhắc đúng nhãn gần báo cáo nhất; margin âm nghĩa là có ít nhất một câu nhắc nhãn khác gần hơn.
    \item \textbf{Tổng hợp:} lấy trung bình $m_i$ trên toàn bộ tập dữ liệu để đánh giá liệu tên bệnh đúng có thể được truy hồi ổn định từ nội dung văn bản hay không.
\end{enumerate}

Bảng~\ref{tab:appendix-btxrd-label-leakage} chỉ giữ hai kết quả có diễn giải trực tiếp đối với nguy cơ rò rỉ nhãn; các kết quả trung gian được lược bỏ để tránh gây nhầm lẫn.

\begin{table}[H]
\centering
\small
\begin{tabular}{|L{8.6cm}|r|}
\hline
\textbf{Phép kiểm tra} & \textbf{Kết quả} \\ \hline
Báo cáo chứa trực tiếp tên bệnh đích trong danh sách rà soát & 0/3.746 \\ \hline
Cosine margin trung bình giữa đúng nhãn và nhãn khác gần nhất & $-0{,}0400$ \\ \hline
\end{tabular}
\caption{Kiểm tra nguy cơ rò rỉ nhãn trực tiếp và gián tiếp trong văn bản BTXRD}
\label{tab:appendix-btxrd-label-leakage}
\end{table}

Không phát hiện trường hợp tên bệnh đích xuất hiện trực tiếp trong văn bản. Cosine margin trung bình bằng $-0{,}0400$, tức là độ tương đồng với đúng nhãn thấp hơn nhãn khác gần nhất trung bình 0,04 đơn vị cosin. Vì vậy, phép kiểm tra không cho thấy tên bệnh cụ thể có thể được suy ra trực tiếp và ổn định từ báo cáo tổng hợp. Kết quả này chỉ đánh giá nguy cơ lộ tên nhãn; không đánh giá tính đúng đắn lâm sàng hay hình thái của nội dung văn bản.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=4mm,
    flowbox/.style={draw, rounded corners, align=left, text width=0.8\textwidth, inner sep=5pt, fill=gray!7},
    flowarrow/.style={-{Latex[length=2.4mm]}, semithick}
]
\node[flowbox] (metadata) {\textbf{1. Hàng siêu dữ liệu}\quad \texttt{IMG000001.jpeg}: tuổi 48, giới tính F, \texttt{hip bone}=1, \texttt{pelvis}=1, \texttt{tumor}=1, \texttt{malignant}=1 và \texttt{frontal}=1.};

\node[flowbox, below=of metadata] (rules) {\textbf{2. Suy ra điều kiện}\quad Vị trí chính: \textit{hip bone}; nhóm tuổi: 35--49; nhóm mẫu câu: ác tính; tư thế: AP; không có nhiều xương hoặc khớp liên quan.};

\node[flowbox, below=of rules] (selection) {\textbf{3. Chọn đều với hạt giống 42}\quad Khoảng thời gian: \textit{1 month}; lý do: \textit{Pain and swelling of the hip bone}; diễn tiến: \textit{Pain in the hip bone for 1 month; associated with decreased appetite}; khám: \textit{Palpable mass; tenderness on palpation; mild swelling}; tiền sử: \textit{No prior bone disease or fracture} và \textit{No relevant surgical or medical history}.};

\node[flowbox, below=of selection] (assembly) {\textbf{4. Ghép theo thứ tự cố định}\quad \textit{Patient: 48-year-old female. Reason for admission: Pain and swelling of the hip bone. Disease history: Pain in the hip bone for 1 month; associated with decreased appetite. Physical examination: Palpable mass; tenderness on palpation; mild swelling. Personal medical history: No prior bone disease or fracture. No relevant surgical or medical history. Radiographic evaluation: AP view of the hip bone was obtained.}};

\node[flowbox, below=of assembly] (storage) {\textbf{5. Cố định đầu vào}\quad Lưu thành \texttt{IMG000001.txt}};

\draw[flowarrow] (metadata) -- (rules);
\draw[flowarrow] (rules) -- (selection);
\draw[flowarrow] (selection) -- (assembly);
\draw[flowarrow] (assembly) -- (storage);
\end{tikzpicture}
\caption{Quá trình tạo văn bản minh họa cho mẫu \texttt{IMG000001.jpeg}}
\label{fig:appendix-btxrd-example-flow}
\end{figure}

\section{Bộ dữ liệu CTCH}
\label{AppendixCTCH}

Phụ lục này mô tả quy trình chuẩn bị CTCH nhằm hỗ trợ tái lập và đánh giá tính hợp lệ của dữ liệu. Nội dung tập trung vào tiêu chí lựa chọn mẫu, cách hình thành nhãn, nguyên tắc chia tập, nguồn gốc văn bản và các phép kiểm tra toàn vẹn.

\subsubsection{Sàng lọc và xác định quần thể nghiên cứu}

Quần thể đích của bộ dữ liệu CTCH gồm các bệnh nhân được chụp X-quang xương khớp nhằm hỗ trợ chẩn đoán bệnh lý hoặc chấn thương xương. Quần thể tiếp cận bao gồm các bệnh nhân có bệnh án và dữ liệu hình ảnh được thu thập tại Bệnh viện Chấn thương Chỉnh hình Thành phố Hồ Chí Minh.

Dữ liệu ban đầu gồm 12.949 bệnh án. Tuy nhiên, các hồ sơ này chưa thể được sử dụng trực tiếp vì có sự khác biệt về loại dữ liệu cận lâm sàng, thời điểm thăm khám, tình trạng điều trị và mức độ phù hợp của nhãn. Quy trình sàng lọc được thực hiện tuần tự nhằm hình thành quần thể có ảnh X-quang, bệnh sử và nhãn phù hợp với phạm vi phân loại của nghiên cứu. Số lượng hồ sơ còn lại sau từng bước được trình bày trong Hình~\ref{fig:appendix-ctch-screening-flow}.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=4mm,
    flowbox/.style={draw, rounded corners, align=left, text width=0.85\textwidth, inner sep=5pt, fill=gray!7},
    flowarrow/.style={-{Latex[length=2.4mm]}, semithick}
]
\node[flowbox] (step1) {\textbf{1. Dữ liệu bệnh án ban đầu}\quad Tổng số: 12.949 hồ sơ};
\node[flowbox, below=of step1] (step2) {\textbf{2. Các mẫu không có ảnh X-quang}\quad Còn lại: 9.142 hồ sơ};
\node[flowbox, below=of step2] (step3) {\textbf{3. Loại các trường hợp đã mổ}\quad Còn lại: 7.900 hồ sơ};
\node[flowbox, below=of step3] (step4) {\textbf{4. Loại các ca tái khám}\quad Còn lại: 7.070 hồ sơ};
\node[flowbox, below=of step4] (step5) {\textbf{5. Chỉ giữ lần khám đầu tiên}\quad Còn lại: 6.575 hồ sơ};
\node[flowbox, below=of step5] (step6) {\textbf{6. Lọc và chuẩn hóa nhãn}\quad Còn lại: 3.641 hồ sơ};
\node[flowbox, below=of step6] (step7) {\textbf{7. Loại các hồ sơ có dụng cụ y tế}\quad Còn lại: 3.293 hồ sơ};
\draw[flowarrow] (step1) -- (step2);
\draw[flowarrow] (step2) -- (step3);
\draw[flowarrow] (step3) -- (step4);
\draw[flowarrow] (step4) -- (step5);
\draw[flowarrow] (step5) -- (step6);
\draw[flowarrow] (step6) -- (step7);
\end{tikzpicture}
\caption{Quy trình sàng lọc hồ sơ cho bộ dữ liệu CTCH}
\label{fig:appendix-ctch-screening-flow}
\end{figure}

\subsection{Cấu trúc dữ liệu và đặc điểm kỹ thuật}

Bảng~\ref{tab:appendix-ctch-storage} mô tả các thành phần liên quan đến phạm vi phân loại của nghiên cứu. Định danh ảnh là khóa liên kết một-một giữa siêu dữ liệu, nhãn, tập chia, ảnh và bệnh sử lâm sàng.

\begin{table}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{|l|l|l|}
\hline
\textbf{Thành phần}                    & \textbf{Số lượng sử dụng} & \textbf{Vai trò}                          \\ \hline
\texttt{images/}          & 3.293            & Lưu ảnh X-quang đầu vào được liên kết bởi manifest.                  \\ \hline
\texttt{reports/} &
  \begin{tabular}[c]{@{}l@{}}3.293 cặp song ngữ \\ (6.586 tệp)\end{tabular}  &
  \begin{tabular}[c]{@{}l@{}}Mỗi ảnh được chọn có một bệnh sử lâm sàng \\ tiếng Việt và một bản tiếng Anh tương ứng.\end{tabular} \\ \hline
\texttt{ctch-labels.csv} & 3.249           & Chứa định danh ảnh và 22 cột chỉ báo lớp. \\ \hline
\texttt{ctch-ood.csv} & 44           & Chứa định danh và thông tin các mẫu ngoài phân phối. \\ \hline
\texttt{ctch-split.csv} &
  3.249  &
  \begin{tabular}[c]{@{}l@{}}Ánh xạ mỗi ảnh vào \texttt{train}, \\ \texttt{validate} hoặc \texttt{test}.\end{tabular} \\ \hline
\end{tabular}%
}
\caption{Cấu trúc lưu trữ của bộ dữ liệu CTCH trong nghiên cứu}
\label{tab:appendix-ctch-storage}
\end{table}

Bảng~\ref{tab:appendix-ctch-image-properties} cho thấy dữ liệu ảnh có định dạng đồng nhất. Toàn bộ 3.293 tệp lưu trữ đều có phần mở rộng JPEG và được mã hóa chuẩn JPEG với toàn bộ 3.293 ảnh giải mã thành công. Khi nạp dữ liệu, mỗi ảnh được chuyển sang RGB trước khi tạo tensor, bảo đảm đầu vào ba kênh cho mô hình.

\begin{table}[H]
\centering
\caption{Đặc điểm kỹ thuật của các tệp ảnh CTCH}
\label{tab:appendix-ctch-image-properties}
\small
\resizebox{\textwidth}{!}{%
\begin{tabularx}{\textwidth}{|L{4.2cm}|X|}
\hline
\textbf{Thuộc tính} & \textbf{Kết quả kiểm tra} \\ \hline
Phần mở rộng & 3.293 tệp \texttt{.jpg}. \\ \hline
Chế độ màu khi giải mã & 3.293 ảnh \texttt{RGB}. \\ \hline
Định dạng mã hóa & 3.293 JPEG. \\ \hline
Chiều rộng & Nhỏ nhất 1.186 px, trung vị 1.213 px, lớn nhất 2.836 px. \\ \hline
Chiều cao & Nhỏ nhất 1.213 px, trung vị 1.713 px, lớn nhất 2.880 px. \\ \hline
Khả năng giải mã & 3.293/3.293 tệp được đọc thành công. \\ \hline
\end{tabularx}%
}
\end{table}

Dữ liệu nguồn gồm 3.641 hồ sơ đã được lựa chọn sơ bộ từ các ca khám tại Bệnh viện Chấn thương Chỉnh hình Thành phố Hồ Chí Minh. Mỗi hồ sơ liên kết thông tin bệnh án với một ảnh đại diện cho vị trí cần khảo sát. Quá trình sàng lọc tiếp tục loại những ca có nẹp, đinh hoặc phương tiện kết hợp xương vì các cấu trúc này có thể che khuất tổn thương và tạo dấu hiệu thị giác ngoài mục tiêu phân loại.

Hiện tại 348 hồ sơ có dụng cụ y tế bị loại nhằm đảm bảo phạm vi nghiên cứu, đồng thời giúp đảm bảo chất lượng dữ liệu đầu vào là đồng nhất. Phần còn lại gồm 3.293 mẫu, được tách thành 3.249 mẫu trong phân phối phục vụ phân loại và 44 mẫu ngoài phân phối về ngữ nghĩa. Hai quần thể không giao nhau theo ảnh hoặc theo mã nhóm người bệnh.

\begin{table}[H]
\centering
\caption{Dòng dữ liệu qua các bước sàng lọc CTCH}
\label{tab:appendix-ctch-cohort-flow}
\small
\begin{tabular}{|l|r|}
\hline
\textbf{Giai đoạn} & \textbf{Số hồ sơ} \\ \hline
Hồ sơ nguồn & 3.641 \\ \hline
Loại do có dụng cụ y tế & 348 \\ \hline
Còn lại sau sàng lọc & 3.293 \\ \hline
Quần thể trong phân phối & 3.249 \\ \hline
Quần thể ngoài phân phối & 44 \\ \hline
\end{tabular}%
\end{table}

Quần thể ngoài phân phối gồm 34 mẫu thuộc nhóm chấn thương và 10 mẫu thuộc nhóm viêm nhiễm, nhưng biểu hiện cụ thể không thuộc 22 lớp đích. Cách tổ chức này tạo một kịch bản ngoài dữ liệu trong cùng nguồn bệnh viện, qua đó giảm mức trộn lẫn giữa sai lệch ngữ nghĩa với thay đổi thiết bị hoặc cơ sở thu nhận. Tuy nhiên, số lượng 44 mẫu còn nhỏ nên khoảng bất định của các độ đo ngoài phân phối cần được trình bày và kết quả không nên được khái quát thành mọi dạng bất thường lâm sàng.


\subsubsection{Nguồn văn bản và kiểm soát rò rỉ nhãn}

Văn bản đầu vào CTCH được tạo ngoại tuyến từ ba trường có sẵn trước kết luận chẩn đoán cuối cùng gồm lý do vào viện, diễn tiến bệnh và tiền sử. Các trường kết quả khảo sát hình ảnh, chẩn đoán chuyên môn và nhãn phân lớp không được nối vào chuỗi văn bản đầu vào như thể hiện trong Bảng \ref{tab:appendix-ctch-text-fields}. Chẩn đoán chuyên môn chỉ được sử dụng trong quá trình chuẩn hóa và dùng làm nhãn đích; nhãn phân loại chỉ tham gia kiểm tra và xác minh nhãn.

\begin{table}[H]
\centering
\small
\resizebox{\textwidth}{!}{%
\begin{tabularx}{\textwidth}{|L{4.1cm}|C{2.4cm}|X|}
\hline
\textbf{Trường dữ liệu nguồn} & \textbf{Dùng làm đầu vào} & \textbf{Vai trò trong nghiên cứu} \\ \hline
Lý do vào viện & Có & Cung cấp triệu chứng hoặc vấn đề chính được ghi nhận khi tiếp nhận. \\ \hline
Diễn tiến bệnh & Có & Cung cấp cơ chế, thời điểm và diễn biến trước khi hình thành kết luận chẩn đoán. \\ \hline
Tiền sử & Có & Cung cấp thông tin bệnh lý, phẫu thuật hoặc điều trị trước đó khi trường này có dữ liệu. \\ \hline
Kết quả khảo sát hình ảnh & Không & Bị loại khỏi chuỗi đầu vào vì có thể chứa mô tả trực tiếp dấu hiệu trên phim. \\ \hline
Chẩn đoán chuyên môn & Không & Chỉ dùng để chuẩn hóa, ánh xạ và dùng làm nhãn phân loại chính; không được mã hóa bởi nhánh văn bản. \\ \hline
Nhãn phân lớp & Không & Dùng để kiểm tra và xác minh nhãn. \\ \hline
\end{tabularx}%
}
\caption{Vai trò của các trường bệnh án CTCH và nguyên tắc loại thông tin có nguy cơ rò rỉ nhãn}
\label{tab:appendix-ctch-text-fields}
\end{table}

Trong bước tạo thư mục \texttt{reports/}, ba trường được giữ lại được làm sạch và ghép theo thứ tự cố định; không có tên lớp, kết quả đọc ảnh hoặc chẩn đoán được bổ sung từ tệp nhãn. Việc chia tập được thực hiện theo mã người bệnh trước khi huấn luyện, còn mã liên kết không được đưa vào văn bản hoặc mô hình. Cách tổ chức này loại rò rỉ trực tiếp từ các cột chẩn đoán, nhưng không loại bỏ khả năng bệnh sử chứa những triệu chứng hoặc vị trí giải phẫu có tính dự báo cao; vì vậy, khóa luận vẫn báo cáo đối chứng chỉ văn bản và phép ghép sai để khảo sát mức phụ thuộc vào nguồn này.

\paragraph{Công cụ dịch tiếng Anh.}
Bệnh sử tiếng Anh được tạo ngoại tuyến từ bản tiếng Việt bằng gói \texttt{deep-translator} phiên bản 1.11.4 với lớp \texttt{GoogleTranslator}, thiết lập ngôn ngữ nguồn là tiếng Việt (\texttt{vi}) và ngôn ngữ đích là tiếng Anh (\texttt{en}) \cite{DeepTranslator}. Nội dung của từng trường được dịch riêng, trong khi tên trường, thứ tự trường và định danh liên kết với ảnh được giữ nguyên. Các đoạn tiếng Việt đã dịch được lưu trong một tệp bộ nhớ đệm JSON; khi một đoạn xuất hiện lại, quy trình tái sử dụng đúng bản dịch đã lưu thay vì gửi một yêu cầu mới. Cơ chế này làm cho các câu lặp lại nhận cùng một bản dịch và hỗ trợ tái lập dữ liệu đã tạo.

\paragraph{Đánh giá độ tương đồng ngữ nghĩa của bản dịch.}
Chất lượng bản dịch được sàng lọc tự động bằng mô hình MiniLM đa ngôn ngữ \texttt{paraphrase-multilingual-MiniLM-L12-v2} \cite{ParaphraseMultilingualMiniLM}. Phép đánh giá chỉ chạy trên 3.293 ảnh còn lại sau sàng lọc và có tệp ảnh hợp lệ, không chạy trên các tệp vật lý ngoài manifest. Với mỗi trường có nội dung ở cả hai ngôn ngữ, câu tiếng Việt và câu tiếng Anh được mã hóa riêng với độ dài tối đa 256 token. Vector câu được tạo bằng trung bình có mặt nạ trên các token hợp lệ, sau đó chuẩn hóa theo chuẩn $L_2$ và tính cosine similarity giữa hai ngôn ngữ. Điểm càng gần 1 biểu thị hai biểu diễn càng gần nhau về ngữ nghĩa. Ngưỡng 0,70 chỉ được dùng để gắn cờ các cặp cần rà soát, không được xem là tiêu chuẩn tuyệt đối về tính đúng đắn y khoa.

Trung bình phản ánh mức điểm chung nhưng nhạy với phần đuôi điểm thấp, còn trung vị mô tả vị trí trung tâm bền vững hơn trước các trường hợp ngoại lệ. Bảng~\ref{tab:appendix-ctch-translation-similarity} báo cáo cả hai đại lượng trên 9.542 cặp trường song ngữ được liên kết.

\begin{table}[H]
\centering
\caption{Độ tương đồng ngữ nghĩa giữa bệnh sử tiếng Việt và bản dịch tiếng Anh của CTCH}
\label{tab:appendix-ctch-translation-similarity}
\small
\begin{tabular}{|L{5.0cm}|r|r|r|r|}
\hline
\textbf{Trường bệnh sử} & \textbf{Số cặp} & \textbf{Trung bình} & \textbf{Trung vị} & \textbf{Dưới 0,70} \\ \hline
Lý do vào viện & 3.293 & 0,7264 & 0,7767 & 1.083 (32,89\%) \\ \hline
Diễn tiến bệnh & 3.293 & 0,7462 & 0,7747 & 839 (25,48\%) \\ \hline
Tiền sử cá nhân & 2.956 & 0,5330 & 0,4737 & 2.474 (83,69\%) \\ \hline
Toàn bộ & 9.542 & 0,6733 & 0,7234 & 4.396 (46,07\%) \\ \hline
\end{tabular}
\end{table}

Ở hai trường cốt lõi, điểm trung bình đạt 0,7264--0,7462 và trung vị đạt 0,7747--0,7767. Các đại lượng trung tâm gần nhau và trung vị xấp xỉ 0,78 cho thấy phần trung tâm của phân bố tương đồng tương đối ổn định. Điểm tiền sử thấp hơn vì trường này có nhiều câu rất ngắn và 1.980 trường hợp thuộc ba biến thể của phát biểu ``chưa phát hiện/chưa ghi nhận bất thường''; bản dịch đúng ý nghĩa nhưng chỉ nhận cosine similarity khoảng 0,41--0,47. Rà soát phần đuôi điểm thấp ghi nhận cả cảnh báo giả lẫn một số lỗi thuật ngữ cần sửa. Do đó, trung bình và trung vị cung cấp bằng chứng định lượng rằng bản dịch tiếng Anh nhìn chung giữ được ngữ nghĩa, đặc biệt ở lý do vào viện và diễn tiến bệnh, nhưng vẫn cần kiểm tra thủ công bởi người có chuyên môn.

\subsubsection{Kiểm chứng liên kết, nhãn và tập chia}

Khác với BTXRD, bệnh sử CTCH là dữ liệu được trích từ bệnh án nên không thể kiểm chứng bằng cách phát lại một ngân hàng mẫu câu. Phép kiểm tra phù hợp là đối chiếu khóa định danh giữa manifest, ảnh, văn bản và nhãn; xác nhận tính một-nóng của nhãn; đồng thời kiểm tra giao nhau theo người bệnh giữa các tập dữ liệu. Kết quả tính trực tiếp trên quần thể được manifest lựa chọn được trình bày trong Bảng~\ref{tab:appendix-ctch-integrity-verification}.

\begin{table}[H]
\centering
\small
\begin{tabular}{|L{8.2cm}|r|}
\hline
\textbf{Phép kiểm tra} & \textbf{Kết quả} \\ \hline
Định danh trong phân phối là duy nhất & 3.249/3.249 (100\%) \\ \hline
Định danh ngoài phân phối là duy nhất & 44/44 (100\%) \\ \hline
Giao nhau theo ảnh giữa quần thể trong và ngoài phân phối & 0 mẫu \\ \hline
Giao nhau theo người bệnh giữa quần thể trong và ngoài phân phối & 0 người bệnh \\ \hline
Mẫu được chọn có ảnh giải mã được & 3.293/3.293 (100\%) \\ \hline
Mẫu được chọn có đủ bệnh sử tiếng Việt và tiếng Anh & 3.293/3.293 (100\%) \\ \hline
Tệp bệnh sử được liên kết có nội dung khác rỗng & 6.586/6.586 (100\%) \\ \hline
Nhãn trong phân phối có đúng một lớp trong 22 lớp & 3.249/3.249 (100\%) \\ \hline
\texttt{class\_id}, tên lớp và cột một-nóng nhất quán & 3.249/3.249 (100\%) \\ \hline
Giao nhau theo người bệnh giữa \texttt{train}/\texttt{validate}/\texttt{test} & 0/0/0 \\ \hline
Quy mô \texttt{train}/\texttt{validate}/\texttt{test} & 2.265/315/669 \\ \hline
\end{tabular}
\caption{Kết quả kiểm chứng tính toàn vẹn của quần thể CTCH được sử dụng}
\label{tab:appendix-ctch-integrity-verification}
\end{table}

Trong 3.293 bệnh sử tiếng Anh được liên kết, trường lý do vào viện và diễn tiến bệnh có nội dung ở toàn bộ mẫu; trường tiền sử có nội dung ở 2.956 mẫu (89,77\%) và không có dữ liệu ở 337 mẫu. Hai con số tương ứng giống nhau ở nguồn tiếng Việt, nên tổng cộng có 5.912/6.586 tệp (89,77\%) chứa nội dung tiền sử. Việc thiếu tiền sử được giữ như trạng thái dữ liệu nguồn, không được nội suy bằng câu giả lập.
\input{Appendix/model_details}

\chapter{Kết quả chi tiết theo từng hạt giống}
\label{AppendixSeedResults}

\section{Phạm vi trình bày}

Phụ lục này công bố các kết quả riêng của ba hạt giống ngẫu nhiên 42, 123 và 456 nhằm bổ trợ cho các giá trị trung bình và độ lệch chuẩn được trình bày trong Chương~\ref{Chapter4}. Sự thay đổi giữa các hạt giống được sử dụng chủ yếu để đánh giá độ nhạy của quy trình huấn luyện và hỗ trợ việc diễn giải khoảng tin cậy, thay vì khẳng định phân phối hiệu năng của mô hình.

Các kết quả gồm phân loại khi sử dụng toàn bộ dữ liệu, phân loại trong trường hợp mẫu học hạn chế, nghiên cứu loại bỏ từng thành phần và đánh giá dữ liệu ngoài phân phối bằng hậu xử lý. Đây là các nội dung trong Chương~\ref{Chapter4} có đủ kết quả cho cả ba hạt giống và có thể đối chiếu trực tiếp với các bảng tổng hợp. Kết quả không mẫu, phép đo hiệu quả suy luận và hai trường hợp minh họa bằng phương pháp tích phân gradient không được lặp lại dưới dạng bảng ba hạt giống vì các nội dung này hiện chỉ được thực hiện một lần hoặc được dùng như minh họa định tính.

\section{Phân loại khi sử dụng toàn bộ dữ liệu huấn luyện}

Bảng~\ref{tab:appendix-full-shot-seeds-btxrd} và Bảng~\ref{tab:appendix-full-shot-seeds-ctch} trình bày năm độ đo phân loại của từng mô hình tại từng hạt giống. Việc giữ đồng thời Accuracy, Balanced Accuracy, Macro-F1, Macro-AUROC và Macro-AUPRC cho phép kiểm tra liệu thứ hạng trung bình trong Chương~\ref{Chapter4} có xuất hiện nhất quán hay phụ thuộc vào một lần khởi tạo cụ thể.

\input{tables/appendix/table_full_shot_per_seed}

Hình~\ref{fig:appendix-full-shot-f1-seeds} tập trung vào XBone-Net và hai mô hình cơ sở PEFT gần nhất để minh họa biến thiên Macro-F1 mà không lặp lại toàn bộ thông tin trong hai bảng. Trên BTXRD, đường trung bình quan sát của XBone-Net nằm dưới hai mô hình cơ sở PEFT ở một số hạt giống; tuy nhiên, khoảng tin cậy và p-value ghép cặp trong Chương~4 không cho thấy khác biệt Macro-F1 nhất quán. Trên CTCH, ba đường giao nhau giữa các hạt giống và kết quả kiểm định cũng cho thấy Macro-F1 của XBone-Net cạnh tranh với hai mô hình này.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/appendix/full_shot_macro_f1_by_seed.png}
\caption{Macro-F1 khi sử dụng toàn bộ dữ liệu của XBone-Net và hai mô hình cơ sở PEFT tại từng hạt giống trên BTXRD và CTCH}
\label{fig:appendix-full-shot-f1-seeds}
\end{figure}

Hình~\ref{fig:appendix-full-shot-f1-seeds} chỉ trình bày điểm Macro-F1 tại từng hạt giống để minh họa độ biến thiên giữa các lần huấn luyện. Forest plot Macro-F1 được giữ trong phần kết quả chính tại Hình~\ref{fig:full_shot_f1_forest}. Bốn forest plot bổ sung ở Hình~\ref{fig:appendix-full-shot-accuracy-forest}--\ref{fig:appendix-full-shot-auprc-forest} báo cáo Accuracy, Balanced Accuracy, Macro-AUROC và Macro-AUPRC. Các khoảng tin cậy 95\% được ước lượng bằng bootstrap phân tầng theo bệnh nhân và hạt giống, còn p-value thô được tính bằng kiểm định hoán vị ghép cặp.

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/full_shot_forest_accuracy.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Accuracy giữa XBone-Net và các mô hình cơ sở trong thiết lập toàn bộ dữ liệu}
\label{fig:appendix-full-shot-accuracy-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/full_shot_forest_balanced_accuracy.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Balanced Accuracy giữa XBone-Net và các mô hình cơ sở trong thiết lập toàn bộ dữ liệu}
\label{fig:appendix-full-shot-balanced-accuracy-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/full_shot_forest_auroc_macro.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Macro-AUROC giữa XBone-Net và các mô hình cơ sở trong thiết lập toàn bộ dữ liệu}
\label{fig:appendix-full-shot-auroc-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/full_shot_forest_auprc_macro.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Macro-AUPRC giữa XBone-Net và các mô hình cơ sở trong thiết lập toàn bộ dữ liệu}
\label{fig:appendix-full-shot-auprc-forest}
\end{figure}

\section{Phân loại trong trường hợp mẫu học hạn chế}

Bảng~\ref{tab:appendix-few-shot-seeds-btxrd} và Bảng~\ref{tab:appendix-few-shot-seeds-ctch} báo cáo kết quả tại từng hạt giống cho các mức ngân sách tối đa 1, 10 và 20 mẫu trên mỗi lớp. Khi số mẫu huấn luyện của một lớp thấp hơn ngân sách, toàn bộ mẫu hiện có được giữ lại và không lấy lặp; vì vậy mức 20 mẫu của CTCH sử dụng 19 mẫu cho lớp gãy xương thuyền và 16 mẫu cho lớp gãy xương vùng chậu. Các giá trị này bổ sung cho biểu đồ trung bình trong Chương~\ref{Chapter4}, đồng thời cho phép nhận diện trường hợp chênh lệch trung bình nhỏ hơn mức dao động giữa các lần huấn luyện.

\input{tables/appendix/table_few_shot_per_seed}

Forest plot Macro-F1 của thiết lập ít mẫu được giữ trong phần kết quả chính tại Hình~\ref{fig:few_shot_f1_forest}. Các forest plot bổ sung ở Hình~\ref{fig:appendix-few-shot-accuracy-forest}--\ref{fig:appendix-few-shot-auprc-forest} sử dụng khoảng Student-t 95\% và p-value thô của kiểm định t ghép cặp trên ba hạt giống chung. Do chỉ có ba cặp hạt giống, các kết quả này được diễn giải như bằng chứng thăm dò.

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/few_shot_forest_accuracy.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Accuracy trong thiết lập ít mẫu}
\label{fig:appendix-few-shot-accuracy-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/few_shot_forest_balanced_accuracy.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Balanced Accuracy trong thiết lập ít mẫu}
\label{fig:appendix-few-shot-balanced-accuracy-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/few_shot_forest_auroc_macro.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Macro-AUROC trong thiết lập ít mẫu}
\label{fig:appendix-few-shot-auroc-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/few_shot_forest_auprc_macro.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Macro-AUPRC trong thiết lập ít mẫu}
\label{fig:appendix-few-shot-auprc-forest}
\end{figure}

\section{Nghiên cứu loại bỏ từng thành phần}

Bảng~\ref{tab:appendix-ablation-seeds} trình bày kết quả của XBone-Net và năm biến thể loại bỏ từng thành phần tại từng hạt giống trên CTCH. Một biến thể có giá trị trung bình cao hơn nhưng thay đổi dấu chênh lệch giữa các hạt giống không được xem là bằng chứng về ưu thế ổn định.

\input{tables/appendix/table_ablation_per_seed}

Forest plot Macro-F1 của nghiên cứu thành phần được giữ trong phần kết quả chính tại Hình~\ref{fig:ablation_f1_forest}. Các khoảng trong bốn forest plot bổ sung ở Hình~\ref{fig:appendix-ablation-accuracy-forest}--\ref{fig:appendix-ablation-auprc-forest} được tính bằng 10.000 lần tái lấy mẫu bắt cặp; hai cận là phân vị $2{,}5\%$ và $97{,}5\%$ của phân bố chênh lệch XBone-Net trừ biến thể. Trong mỗi lần, người bệnh được tái lấy mẫu theo cụm và phân tầng theo lớp, đồng thời ba hạt giống huấn luyện được tái lấy mẫu có hoàn lại. Cùng một mẫu tái lập được áp dụng cho cấu hình tham chiếu và biến thể. P-value thô được tính bằng kiểm định hoán vị ghép cặp và không được hiệu chỉnh cho nhiều phép so sánh. Khác biệt chỉ được xem là nhất quán khi khoảng tin cậy không chứa 0 và đồng thời $p<0{,}05$.

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/ablation_forest_accuracy.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Accuracy giữa XBone-Net và từng biến thể loại bỏ thành phần trên CTCH}
\label{fig:appendix-ablation-accuracy-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/ablation_forest_balanced_accuracy.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Balanced Accuracy giữa XBone-Net và từng biến thể loại bỏ thành phần trên CTCH}
\label{fig:appendix-ablation-balanced-accuracy-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/ablation_forest_auroc_macro.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Macro-AUROC giữa XBone-Net và từng biến thể loại bỏ thành phần trên CTCH}
\label{fig:appendix-ablation-auroc-forest}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.94\textwidth,height=0.72\textheight,keepaspectratio]{images/appendix/ablation_forest_auprc_macro.png}
\caption{Khoảng tin cậy 95\% và p-value thô của chênh lệch Macro-AUPRC giữa XBone-Net và từng biến thể loại bỏ thành phần trên CTCH}
\label{fig:appendix-ablation-auprc-forest}
\end{figure}

\section{Kết quả đánh giá dữ liệu ngoài phân phối}

Bảng~\ref{tab:appendix-ood-seeds} công bố kết quả theo từng hạt giống cho bốn phương pháp chấm điểm OOD trên kịch bản OOD ngữ nghĩa và BTXRD. Số mẫu trong phân phối và ngoài phân phối được ghi trực tiếp trong bảng vì AUPR-Out và FPR@95\%TPR chịu ảnh hưởng bởi số lượng và tỷ lệ mẫu của từng kịch bản.

Mỗi lần đánh giá sử dụng 669 mẫu CTCH trong phân phối. Kịch bản OOD ngữ nghĩa sử dụng đủ 44 mẫu trong danh sách đánh giá và kịch bản BTXRD sử dụng đủ 750 mẫu ngoài phân phối; không mẫu nào bị bỏ qua trong các tệp kết quả được tổng hợp. Tuy nhiên, AUPR-Out và FPR@95\%TPR vẫn chịu ảnh hưởng của chênh lệch quy mô và tỷ lệ mẫu trong--ngoài phân phối giữa hai kịch bản, vì vậy cần được đọc cùng AUROC-OOD và cỡ mẫu ghi trong bảng.

\input{tables/appendix/table_ood_per_seed}

Hình~\ref{fig:appendix-ood-mahalanobis-seeds} biểu diễn riêng phương pháp Mahalanobis theo tâm lớp vì đây là phương pháp được nhấn mạnh trong phần kết quả chính. Khoảng biến thiên lớn hơn trên BTXRD, đặc biệt ở AUROC-OOD, cho thấy kết quả liên bộ dữ liệu nhạy với trạng thái biểu diễn được học ở từng hạt giống. Kịch bản OOD ngữ nghĩa có AUROC-OOD ổn định hơn nhưng FPR@95\%TPR vẫn cao ở cả ba lần chạy.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/appendix/ood_mahalanobis_by_seed.png}
\caption{Các độ đo OOD của phương pháp Mahalanobis theo tâm lớp tại từng hạt giống trên kịch bản OOD ngữ nghĩa và BTXRD}
\label{fig:appendix-ood-mahalanobis-seeds}
\end{figure}
