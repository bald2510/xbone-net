\chapter{Thực nghiệm}
\label{chap:experiments_new}

Chương này trình bày thiết lập đánh giá và bản tổng hợp kết quả hiện có của XBone-Net trên hai bộ dữ liệu BTXRD và CTCH. Các thí nghiệm được tổ chức theo năm nhóm: đánh giá zero-shot của mô hình nền tảng, so sánh phân loại với các mô hình đối chứng, đánh giá thành phần, phát hiện dữ liệu ngoài phân phối và phân tích cơ chế dung hợp. Trừ thiết lập zero-shot và các hình được ghi rõ một hạt giống, kết quả trong các bảng là trung bình $\pm$ độ lệch chuẩn trên ba hạt giống 42, 123 và 456. Giá trị tốt nhất trong cùng một bộ dữ liệu hoặc kịch bản được in đậm.

\section{Bộ dữ liệu}

BTXRD gồm 3.746 ảnh X-quang thuộc 10 lớp, được chia thành 2.621 mẫu huấn luyện, 375 mẫu đánh giá và 750 mẫu kiểm thử. Văn bản đi kèm BTXRD được sinh có điều kiện từ các thuộc tính lâm sàng đã công bố; tên bệnh trực tiếp được loại khỏi văn bản trước khi huấn luyện. Vì vậy, kết quả đa phương thức trên BTXRD phản ánh khả năng khai thác thông tin lâm sàng tổng hợp và không được diễn giải như bằng chứng trực tiếp trên báo cáo tự do do bác sĩ viết.

CTCH gồm 400 ảnh X-quang của 400 trường hợp và bệnh sử tiếng Việt tương ứng, được chuẩn hóa thành bài toán phân loại 22 lớp. Số lượng lớp lớn so với quy mô dữ liệu và phân bố lớp không đồng đều khiến CTCH phù hợp để khảo sát khả năng học từ mẫu hạn chế, ảnh hưởng của thông tin bệnh sử và hành vi của đầu phân loại theo tâm lớp. Phân chia huấn luyện, đánh giá và kiểm thử được giữ cố định giữa các mô hình.

\begin{table}[H]
\centering
\small
\begin{tabular}{|l|c|c|c|c|}
\hline
\textbf{Bộ dữ liệu} & \textbf{Số mẫu} & \textbf{Số lớp} & \textbf{Văn bản} & \textbf{Vai trò chính} \\ \hline
BTXRD & 3.746 & 10 & Tổng hợp có điều kiện & Phân loại u xương và domain OOD \\ \hline
CTCH & 400 & 22 & Bệnh sử lâm sàng & Dữ liệu đích và semantic OOD \\ \hline
FracAtlas & Dữ liệu ngoài & 2 & Không sử dụng & Domain OOD ngoại viện \\ \hline
\end{tabular}
\caption{Vai trò của các bộ dữ liệu trong protocol thực nghiệm}
\label{tab:ch4draft_datasets}
\end{table}

\section{Độ đo đánh giá}
\label{sec:evaluation_metrics}
Hai bộ dữ liệu BTXRD \cite{Yao2025} và CTCH được thiết lập dưới dạng bài toán phân loại đa lớp, với BTXRD có 10 lớp và 22 lớp đối với CTCH. Các độ đo được chia thành ba nhóm nhằm tách biệt hiệu năng phân loại, khả năng nhận diện dữ liệu ngoài phân phối và đánh giá cơ chế dung hợp.

\subsection{Độ đo phân loại}

\subsubsection{Accuracy}

Accuracy, hay độ chính xác tổng thể, biểu thị tỷ lệ mẫu có nhãn dự đoán trùng với nhãn thật trên toàn bộ tập đánh giá.

\begin{equation}\operatorname{Accuracy}=\frac{\sum_{c=1}^{C} TP_c}{N}.\label{eq:ch4_accuracy}\end{equation}

Trong đó:
\begin{itemize}
    \item $N$ là tổng số mẫu.
    \item $C$ là tổng số lớp.
    \item $TP_c$ là số mẫu thuộc lớp $c$ được phân loại đúng.
\end{itemize}

Accuracy được sử dụng vì có cách diễn giải trực tiếp và được báo cáo phổ biến trong phân loại ảnh y khoa. Tuy nhiên, độ đo này có thể bị chi phối bởi các lớp có nhiều mẫu, do đó không được sử dụng riêng lẻ mà được đọc cùng Balanced Accuracy và Macro-F1. Giá trị cao hơn biểu thị tỷ lệ dự đoán đúng lớn hơn.

\subsubsection{Balanced Accuracy}

Balanced Accuracy \cite{Brodersen2010BalancedAcc} tính trung bình độ nhạy của từng lớp, nhờ đó mỗi lớp đóng góp như nhau vào kết quả bất kể tần suất xuất hiện trong tập đánh giá.

\begin{equation}\operatorname{Balanced\ Accuracy}=\frac{1}{C}\sum_{c=1}^{C}\frac{TP_c}{TP_c+FN_c}.\label{eq:ch4_balanced_accuracy}\end{equation}

Trong đó:
\begin{itemize}
    \item $C$ là tổng số lớp.
    \item $TP_c$ là số mẫu thuộc lớp $c$ được dự đoán đúng.
    \item $FN_c$ là số mẫu thuộc lớp $c$ nhưng bị dự đoán thành lớp khác.
\end{itemize}

Balanced Accuracy được sử dụng để giảm ảnh hưởng của mất cân bằng lớp trên hai bộ dữ liệu BTXRD và CTCH. Độ đo này cho biết liệu khả năng nhận diện có được duy trì trên các lớp ít mẫu thay vì chủ yếu phản ánh lớp phổ biến. Giá trị cao hơn biểu thị recall trung bình giữa các lớp tốt hơn.

\subsubsection{Macro-F1}

Macro-F1 tổng hợp đồng thời precision và recall của từng lớp, sau đó lấy trung bình không trọng số trên toàn bộ lớp.

\begin{equation}\operatorname{Macro\text{-}F1}=\frac{1}{C}\sum_{c=1}^{C}\frac{2TP_c}{2TP_c+FP_c+FN_c}.\label{eq:ch4_macro_f1}\end{equation}

Trong đó:
\begin{itemize}
    \item $C$ là tổng số lớp.
    \item $TP_c$ là số mẫu thuộc lớp $c$ được dự đoán đúng.
    \item $FP_c$ là số mẫu lớp khác bị dự đoán nhầm thành lớp $c$.
    \item $FN_c$ là số mẫu thuộc lớp $c$ nhưng bị dự đoán thành lớp khác.
\end{itemize} 

Macro-F1 được sử dụng làm độ đo chính vì một lớp chỉ đạt F1 cao khi precision và recall đều được duy trì. Phép trung bình hạn chế việc kết quả của lớp phổ biến che khuất sai số tại lớp hiếm. Giá trị cao hơn biểu thị sự cân bằng tốt hơn giữa hai loại sai số.

\subsubsection{Macro-AUROC}

Macro-AUROC \cite{Fawcett2006ROC} đánh giá khả năng xếp hạng xác suất của từng lớp theo chiến lược một đối với tất cả trên toàn bộ ngưỡng, sau đó lấy trung bình không trọng số trên các lớp hợp lệ. 

\begin{equation}
    \operatorname{TPR_c}=\frac{TP_c}{TP_c+FN_c}.
\end{equation}

\begin{equation}
    \operatorname{FPR_c}=\frac{FP_c}{FP_c+TN_c}.
\end{equation}

\begin{equation}
    \operatorname{AUROC_c}=\int_{0}^{1}\operatorname{TPR}_{c}(\operatorname{FPR}_{c})\,\mathrm{d}\operatorname{FPR}_{c}.
\end{equation}

\begin{equation}\operatorname{Macro\text{-}AUROC}=\frac{1}{\left|\mathcal{C}_{v}\right|}\sum_{c\in\mathcal{C}_{v}}\operatorname{AUROC}_{c}\label{eq:ch4_macro_auroc}\end{equation}

Trong đó:
\begin{itemize}
    \item $\mathcal{C}_{v}$ là tập lớp có cả mẫu dương và mẫu âm, $|\mathcal{C}_{v}|$ là số lớp hợp lệ.
    \item $TP_c$ là số mẫu thuộc lớp $c$ được dự đoán đúng.
    \item $FP_c$ là số mẫu lớp khác bị dự đoán nhầm thành lớp $c$.
    \item $TN_c$ là số mẫu lớp khác được xác nhận đúng không thuộc lớp $c$ .
    \item $FN_c$ là số mẫu thuộc lớp $c$ nhưng bị dự đoán thành lớp khác.
    \item $\operatorname{TPR}_{c}$ là tỷ lệ mô hình phát hiện đúng các mẫu thuộc lớp $c$.
    \item $\operatorname{FPR}_{c}$ là tỷ lệ mô hình báo động nhầm các mẫu không thuộc lớp $c$.
    \item $\operatorname{AUROC}_{c}$ là diện tích dưới đường cong ROC riêng của lớp $c$.
\end{itemize}

Macro-AUROC được sử dụng vì không phụ thuộc vào một ngưỡng quyết định duy nhất và phản ánh chất lượng xếp hạng xác suất. Độ đo này bổ sung cho Macro-F1 nhưng ít nhạy hơn với mất cân bằng mẫu dương/âm, vì vậy cần được đọc cùng Macro-AUPRC. Giá trị cao hơn biểu thị khả năng xếp hạng tốt hơn.

\subsubsection{Macro-AUPRC}

Macro-AUPRC \cite{Davis2006PR} đánh giá sự đánh đổi giữa độ chính xác và độ phủ cho từng lớp theo chiến lược một đối với toàn bộ trên toàn bộ dải ngưỡng, sau đó lấy trung bình không trọng số trên các lớp hợp lệ.

\begin{equation}
    \operatorname{Recall}_{c} = \frac{TP_c}{TP_c + FN_c}.
    \label{eq:ch4_recall_def}
\end{equation}

\begin{equation}
    \operatorname{Precision}_{c} = \frac{TP_c}{TP_c + FP_c}.
    \label{eq:ch4_precision_def}
\end{equation}

\begin{equation}
    \operatorname{AP}_{c} = \sum_{k=1}^{K_c} \left(R_{c,k} - R_{c,k-1}\right) P_{c,k}.
    \label{eq:ch4_ap_class}
\end{equation}

\begin{equation}
    \operatorname{Macro\text{-}AUPRC}=\frac{1}{\left|\mathcal{C}_{v}\right|}\sum_{c\in\mathcal{C}_{v}}\operatorname{AP}_{c}.
    \label{eq:ch4_macro_auprc}
\end{equation}

Trong đó:
\begin{itemize}
    \item $\mathcal{C}_{v}$ là tập lớp có cả mẫu dương và mẫu âm, $|\mathcal{C}_{v}|$ là số lớp hợp lệ.
    \item $TP_c$ là số mẫu thuộc lớp $c$ được dự đoán đúng.
    \item $FP_c$ là số mẫu lớp khác bị dự đoán nhầm thành lớp $c$.
    \item $FN_c$ là số mẫu thuộc lớp $c$ nhưng bị dự đoán thành lớp khác.
    \item $\operatorname{Recall}_c$, $\operatorname{Precision}_c$: Độ phủ và độ chính xác của lớp $c$.
    \item $P_{c,k}, R_{c,k}$: Giá trị Precision và Recall tại điểm ngưỡng thứ $k$.
    \item $K_c$: Số lượng điểm ngưỡng phân loại trên tập dữ liệu đối với lớp $c$.
    \item $\operatorname{AUPRC}_c$: Diện tích dưới đường cong Precision-Recall của lớp $c$.
\end{itemize}

Macro-AUPRC được sử dụng vì nhạy với chất lượng nhận diện lớp dương khi số mẫu dương ít hơn đáng kể số mẫu âm. Độ đo này giúp phát hiện trường hợp AUROC tương đối cao nhưng precision của lớp hiếm vẫn thấp. Giá trị cao hơn biểu thị sự đánh đổi precision--recall tốt hơn.

\subsubsection{Expected Calibration Error}

Expected Calibration Error (ECE) \cite{Guo2017Calibration} đánh giá mức phù hợp giữa độ tin cậy dự đoán và tần suất dự đoán đúng quan sát được. Các mẫu được chia vào $M$ khoảng theo xác suất lớn nhất của mô hình trước khi so sánh độ tin cậy trung bình với độ chính xác thực nghiệm trong từng khoảng.

\begin{equation}\operatorname{ECE}=\sum_{m=1}^{M}\frac{|B_m|}{N}\left|\operatorname{Acc}(B_m)-\operatorname{Conf}(B_m)\right|.\label{eq:ch4_ece}\end{equation}

Trong đó:
\begin{itemize}
    \item $M$ là số khoảng độ tin cậy, báo cáo sử dụng $M=15$.
    \item $|B_m|$ là số mẫu trong khoảng.
    \item $N$ là tổng số mẫu.
    \item $\operatorname{Acc}(B_m)$ là tỷ lệ dự đoán đúng.
    \item $\operatorname{Conf}(B_m)$ là trung bình xác suất lớn nhất trong khoảng.
\end{itemize}

ECE được sử dụng vì một mô hình có Accuracy cao vẫn có thể đưa ra xác suất quá tự tin hoặc thiếu tự tin. Trong bài toán hỗ trợ phân tích ảnh y khoa, mức tin cậy cần được diễn giải thận trọng, đặc biệt khi được dùng để sắp xếp ca hoặc hỗ trợ cơ chế từ chối. Giá trị ECE thấp hơn biểu thị độ hiệu chuẩn tốt hơn, nhưng độ đo vẫn phụ thuộc vào cách chia khoảng.

\subsection{Độ đo phát hiện dữ liệu ngoài phân phối}

Để đánh giá khả năng phát hiện dữ liệu ngoài phân phối (Out-of-Distribution - OOD), gọi $\mathcal{D}_{\mathrm{ID}}$ là tập dữ liệu trong phân phối (có $N_{\mathrm{ID}}$ mẫu) và $\mathcal{D}_{\mathrm{OOD}}$ là tập dữ liệu ngoài phân phối (có $N_{\mathrm{OOD}}$ mẫu). Mọi bộ phát hiện đều được quy đổi nhất quán về hàm tính điểm bất thường $s(x)$: mẫu $x$ có giá trị $s(x)$ càng lớn thì khả năng là mẫu OOD càng cao.

\subsubsection{AUROC-OOD}

AUROC-OOD \cite{Hendrycks2017MSP} (Area Under the Receiver Operating Characteristic Curve) đo lường khả năng phân tách tổng quát giữa mẫu ID và mẫu OOD trên toàn bộ các ngưỡng phát hiện. Về mặt ý nghĩa xác suất, AUROC-OOD chính là xác suất để điểm bất thường của một mẫu OOD ngẫu nhiên lớn hơn điểm bất thường của một mẫu ID ngẫu nhiên:

\begin{equation}
    \operatorname{AUROC}_{\mathrm{OOD}} = \Pr\left(s(x_{\mathrm{OOD}}) > s(x_{\mathrm{ID}})\right) + \frac{1}{2}\Pr\left(s(x_{\mathrm{OOD}}) = s(x_{\mathrm{ID}})\right).
    \label{eq:ch4_auroc_ood}
\end{equation}

Trong đó:
\begin{itemize}
    \item $x_{\mathrm{ID}}, x_{\mathrm{OOD}}$ lần lượt là các mẫu ngẫu nhiên được rút từ tập $\mathcal{D}_{\mathrm{ID}}$ và $\mathcal{D}_{\mathrm{OOD}}$.
    \item $s(x)$ là điểm bất thường của mẫu $x$.
    \item $\Pr(\cdot)$ là hàm xác suất biểu thị thứ tự xếp hạng điểm giữa hai mẫu.
\end{itemize}

AUROC-OOD nhận giá trị từ 0 đến 1 (hoặc $0\%$ đến $100\%$), giá trị càng cao biểu thị khả năng phân biệt ID và OOD càng tốt (mức ngẫu nhiên là $0.5$).

\subsubsection{AUPR-Out}

AUPR-Out \cite{Hendrycks2017MSP,Liang2018ODIN} đo lường diện tích bên dưới đường cong Precision-Recall khi coi các mẫu OOD là lớp tích cực cần phát hiện.

\begin{equation}
    \operatorname{Recall}_{\mathrm{OOD}} = \frac{\text{Số mẫu OOD phát hiện đúng}}{\text{Tổng số mẫu OOD}}.
\end{equation}

\begin{equation}
    \operatorname{Precision}_{\mathrm{OOD}} = \frac{\text{Số mẫu OOD phát hiện đúng}}{\text{Tổng số mẫu bị cảnh báo OOD}}.
\end{equation}

Chỉ số AUPR-Out được tính bằng tổng tích phân của Precision theo Recall:

\begin{equation}
    \operatorname{AUPR\text{-}Out} = \int_{0}^{1} \operatorname{Precision}_{\mathrm{OOD}} \,\mathrm{d}\operatorname{Recall}_{\mathrm{OOD}}.
    \label{eq:ch4_aupr_out}
\end{equation}

Trong đó:
\begin{itemize}
    \item $N_{\mathrm{ID}}, N_{\mathrm{OOD}}$ là tổng số mẫu trong tập ID và OOD.
    \item $\tau$ là ngưỡng điểm bất thường để đưa ra quyết định phân loại OOD.
    \item $\operatorname{Precision}_{\mathrm{OOD}}, \operatorname{Recall}_{\mathrm{OOD}}$ là độ chính xác và độ nhạy phát hiện OOD tại ngưỡng $\tau$.
\end{itemize}

AUPR-Out rất quan trọng trong kịch bản dữ liệu mất cân bằng (khi số mẫu OOD ít hơn nhiều so với ID). Giá trị AUPR-Out càng cao biểu thị mô hình ít bị cảnh báo nhầm (báo động giả) trên tập ID.

\subsubsection{FPR@95\%TPR}

FPR@95\%TPR \cite{Liang2018ODIN,Liu2020EnergyOOD} (False Positive Rate at 95\% True Positive Rate) đo lường tỷ lệ báo động giả (mẫu ID bị nhận nhầm thành OOD) khi hệ thống được cài đặt ngưỡng để đảm bảo phát hiện đúng ít nhất $95\%$ các mẫu OOD.

\begin{equation}
    \operatorname{FPR@95\%TPR} = \frac{|\{x \in \mathcal{D}_{\mathrm{ID}} \mid s(x) > \tau_{95}\}|}{N_{\mathrm{ID}}}.
    \label{eq:ch4_fpr95}
\end{equation}

Trong đó:
\begin{itemize}
    \item $\tau_{95}$ là ngưỡng điểm bất thường được chọn sao cho tỷ lệ phát hiện đúng OOD đạt đúng $95\%$ (tức $\operatorname{Recall}_{\mathrm{OOD}}(\tau_{95}) = 0.95$).
    \item $N_{\mathrm{ID}}$ là tổng số mẫu trong tập trong phân phối $\mathcal{D}_{\mathrm{ID}}$.
    \item $s(x) > \tau_{95}$ là điều kiện mẫu $x$ có điểm bất thường vượt ngưỡng và bị hệ thống cảnh báo là OOD.
\end{itemize}

FPR@95\%TPR phản ánh trực tiếp rủi ro thực tế trong lâm sàng: khi hệ thống cố gắng không bỏ sót bệnh lý ngoài phân phối (đạt độ nhạy $95\%$), có bao nhiêu \% dữ liệu bình thường bị cảnh báo nhầm. Giá trị FPR@95\%TPR càng nhỏ càng tốt.

\subsection{Độ đo phân tích chú ý và dung hợp}

Các độ đo trong nhóm này được tính trên tập mẫu phân tích nhằm mô tả hành vi bên trong mô-đun chú ý chéo. Chúng không được xem là bằng chứng trực tiếp rằng mô hình định vị đúng tổn thương hoặc đưa ra giải thích có quan hệ nhân quả. Hai hướng dung hợp được ký hiệu bởi $\mathcal{M}=\{I\leftarrow T,T\leftarrow I\}$, tương ứng với ngữ cảnh văn bản bổ sung cho ảnh và ngữ cảnh ảnh bổ sung cho văn bản.

\subsubsection{Attention Entropy}

Attention Entropy đo mức phân tán của trọng số chú ý trên các token ngữ cảnh hợp lệ. Entropy được chuẩn hóa theo số token để các chuỗi có độ dài khác nhau có thể được đối chiếu trên cùng thang đo.

\begin{equation}\operatorname{H}_{\mathrm{att}}^{(m)}=\frac{1}{N}\sum_{i=1}^{N}\frac{-\sum_{j=1}^{L_i^{(m)}}a_{ij}^{(m)}\log\left(a_{ij}^{(m)}+\epsilon\right)}{\log L_i^{(m)}}.\label{eq:ch4_attention_entropy}\end{equation}

Trong Công thức \ref{eq:ch4_attention_entropy}, $m\in\mathcal{M}$ là một hướng chú ý, $N$ là số mẫu, $L_i^{(m)}$ là số token key hợp lệ của mẫu $i$, $a_{ij}^{(m)}$ là trọng số chú ý của token thứ $j$ sau khi đã tổng hợp các head và chuẩn hóa để tổng bằng một, còn $\epsilon$ là hằng số dương nhỏ dùng để tránh $\log 0$. Giá trị được chuẩn hóa nằm gần khoảng từ không đến một.

Attention Entropy được sử dụng để phát hiện attention quá tập trung vào rất ít token hoặc gần như phân bố đều trên mọi token. Giá trị thấp biểu thị phân bố tập trung, còn giá trị cao biểu thị phân bố rộng; không có chiều tốt hơn tuyệt đối vì mức tập trung phù hợp phụ thuộc nội dung mẫu và hướng chú ý. Độ đo cần được đọc cùng can thiệp đầu ra thay vì dùng riêng để kết luận về tính giải thích.

\subsubsection{Fusion Contribution}

Fusion Contribution mô tả tỷ phần đóng góp hỗ trợ của từng hướng dung hợp dựa trên mức giảm xác suất của lớp mục tiêu khi hướng đó bị loại bỏ. Chỉ phần giảm dương được sử dụng trong tỷ lệ để một nhánh làm tăng xác suất mục tiêu không bị triệt tiêu bởi một nhánh có tác động ngược chiều.

\begin{equation}\operatorname{FC}_{m}=\frac{\sum_{i=1}^{N}\left[\Delta p_i^{(m)}\right]_{+}}{\sum_{r\in\mathcal{M}}\sum_{i=1}^{N}\left[\Delta p_i^{(r)}\right]_{+}+\epsilon}.\label{eq:ch4_fusion_contribution}\end{equation}

Trong Công thức \ref{eq:ch4_fusion_contribution}, $\operatorname{FC}_{m}$ là tỷ phần đóng góp của hướng $m$, $\mathcal{M}$ là tập hai hướng dung hợp, $N$ là số mẫu, $\Delta p_i^{(m)}$ là độ giảm xác suất sau can thiệp trên mẫu $i$, $[z]_{+}=\max(z,0)$ giữ lại phần dương và $\epsilon$ là hằng số nhỏ bảo đảm mẫu số khác không.

Fusion Contribution được sử dụng để so sánh mức phụ thuộc tương đối của dự đoán vào hai hướng truyền thông tin. Giá trị lớn hơn cho một hướng chỉ biểu thị hướng đó chiếm tỷ phần lớn hơn trong tổng tác động hỗ trợ đã quan sát; độ đo không chứng minh phương thức tương ứng quan trọng hơn về mặt lâm sàng và không thay thế đánh giá ablation trên tập kiểm thử.

\subsubsection{Attention Context Cosine}

Attention Context Cosine đo mức đồng hướng giữa hai vector ngữ cảnh do hai nhánh chú ý chéo tạo ra trước khi chúng được đưa vào MLP dung hợp.

\begin{equation}\operatorname{Cos}_{\mathrm{ctx}}=\frac{1}{N}\sum_{i=1}^{N}\frac{\left(\boldsymbol{c}_{i}^{I\leftarrow T}\right)^{\mathsf{T}}\boldsymbol{c}_{i}^{T\leftarrow I}}{\left\|\boldsymbol{c}_{i}^{I\leftarrow T}\right\|_{2}\left\|\boldsymbol{c}_{i}^{T\leftarrow I}\right\|_{2}}.\label{eq:ch4_attention_context_cosine}\end{equation}

Trong Công thức \ref{eq:ch4_attention_context_cosine}, $N$ là số mẫu, $\boldsymbol{c}_{i}^{I\leftarrow T}$ là ngữ cảnh văn bản được truy hồi cho nhánh ảnh, $\boldsymbol{c}_{i}^{T\leftarrow I}$ là ngữ cảnh ảnh được truy hồi cho nhánh văn bản, ký hiệu $\mathsf{T}$ biểu thị chuyển vị và $\|\cdot\|_2$ là chuẩn Euclid.

Attention Context Cosine được sử dụng để khảo sát liệu hai hướng chú ý tạo thông tin tương đồng, gần trực giao hay đối nghịch trong không gian biểu diễn. Giá trị gần một biểu thị hai context đồng hướng, gần không biểu thị mức đồng hướng thấp và giá trị âm biểu thị xu hướng đối nghịch. Không có chiều tốt hơn tuyệt đối vì hai nhánh có thể hữu ích khi mang thông tin bổ sung thay vì lặp lại nhau.

\subsubsection{Prediction Drop after Intervention}

Prediction Drop after Intervention đo thay đổi xác suất của lớp mục tiêu khi một nhánh dung hợp bị đặt về không trong khi các thành phần còn lại được giữ nguyên. Lớp mục tiêu là lớp được mô hình đầy đủ dự đoán trước can thiệp.

\begin{equation}\operatorname{PD}_{m}=\frac{1}{N}\sum_{i=1}^{N}\left[p_{i,\widehat{y}_i}^{\mathrm{full}}-p_{i,\widehat{y}_i}^{(-m)}\right].\label{eq:ch4_prediction_drop}\end{equation}

Trong Công thức \ref{eq:ch4_prediction_drop}, $\operatorname{PD}_{m}$ là độ giảm xác suất trung bình khi loại bỏ hướng $m$, $N$ là số mẫu, $\widehat{y}_i$ là lớp dự đoán của mô hình đầy đủ, $p_{i,\widehat{y}_i}^{\mathrm{full}}$ là xác suất của lớp đó trước can thiệp và $p_{i,\widehat{y}_i}^{(-m)}$ là xác suất sau khi vector của hướng $m$ bị đặt về không.

Prediction Drop được sử dụng để kiểm tra liệu tín hiệu được mô-đun dung hợp tạo ra có ảnh hưởng thực tế đến đầu ra hay không. Giá trị dương lớn cho thấy việc loại bỏ nhánh làm giảm độ tin cậy của dự đoán ban đầu, giá trị gần không cho thấy tác động nhỏ và giá trị âm cho thấy xác suất tăng sau khi loại bỏ. Vì can thiệp có thể tạo biểu diễn khác phân phối huấn luyện, kết quả được diễn giải như bằng chứng chức năng bổ trợ thay vì quan hệ nhân quả đầy đủ.

\section{Thiết lập thực nghiệm}
Phần này trình bày các thiết lập thực nghiệm, bao gồm môi trường phần cứng và phần mềm, cũng như các siêu tham số được sử dụng trong quá trình huấn luyện mô hình. Các thiết lập này nhằm đảm bảo tính nhất quán và khả năng tái tạo của các thí nghiệm.
\subsection{Môi trường thực nghiệm}
Mô hình đề xuất được chạy ba lần trong các điều kiện và siêu tham số giống nhau. Môi trường phần cứng được trình bày trong Bảng
\ref{tab:hardware_environment}.

\begin{table}[htbp]
\centering
\caption{Môi trường phần cứng được thiết lập}
\label{tab:hardware_environment}
\begin{tabular}{|l|c|}
\hline
\textbf{Thành phần} & \textbf{Cấu hình} \\ \hline
GPU & 1 $\times$ RTX 5060 Ti 16 GB VRAM \\ \hline
CPU & Intel Core Ultra 7 265KF \\ \hline
Bộ nhớ hệ thống & 32 GB RAM \\ \hline
Độ chính xác số học & BF16 trong cả hai pha huấn luyện \\ \hline
\end{tabular}
\end{table}

Môi trường phần mềm được cố định theo tệp phụ thuộc của dự án và được tóm tắt
trong Bảng \ref{tab:software_environment}.
\begin{table}[htbp]
\centering
\caption{Phiên bản của các thư viện chính trong môi trường thí nghiệm}
\label{tab:software_environment}
\begin{tabular}{|l|c|}
\hline
\textbf{Thành phần} & \textbf{Phiên bản} \\ \hline
Python & 3.11.15 \\ \hline
Torch & 2.11.0+cu128 \\ \hline
CUDA runtime & 12.8 \\ \hline
\end{tabular}
\end{table}

Đối với phân tích hiệu quả suy luận, năm đại lượng được báo cáo gồm tổng số và số tham số có thể huấn luyện (Params), số phép toán trên một mẫu (GFLOPs), thông lượng (mẫu/giây), độ trễ suy luận (ms/mẫu) và bộ nhớ GPU cực đại (MiB). Độ trễ được tổng hợp bằng giá trị trung bình, P50 và P95 sau các lượt khởi động; bộ nhớ GPU cực đại là lượng bộ nhớ CUDA được cấp phát lớn nhất trong một lượt truyền thuận. Thời gian nạp dữ liệu, tiền xử lý trên CPU và truyền dữ liệu từ CPU sang GPU không được tính vào độ trễ. Mọi mô hình được đo trên cùng GPU, độ chính xác số học, kích thước lô, số lượt khởi động và số lần lặp; với các mô hình zero-shot, biểu diễn prompt được tính trước và không được tính vào thời gian suy luận từng ảnh. Thiết lập này nhằm phản ánh chi phí của chính mô hình và duy trì tính nhất quán giữa các phép đối sánh.

\subsection{Tham số thực nghiệm}
Các Bảng \ref{tab:hyperparameters_phase1} và \ref{tab:hyperparameters_phase2} dưới đây mô tả cấu hình siêu tham số của mô hình đề xuất. Các siêu tham số được lựa chọn dựa trên các nghiên cứu trước và thử nghiệm sơ bộ để tối ưu hóa hiệu suất mô hình trên cả hai bộ dữ liệu BTXRD và CTCH. Các siêu tham số này bao gồm độ phân giải ảnh đầu vào, bộ tối ưu hóa, tốc độ học, suy giảm trọng số, bộ điều chỉnh tốc độ học, tỷ lệ khởi động, kích thước lô, số epoch tối đa, số epoch dừng sớm, nhiệt độ tương phản ($\tau$), trạng thái tham số Backbone, hạng LoRA ($r$), hệ số LoRA ($\alpha$) và tỷ lệ bỏ học LoRA.

\begin{table}[htbp]
\centering
\begin{tabular}{|l|c|}
\hline
\textbf{Siêu tham số}        & \textbf{Pha 1}      \\ \hline
Độ phân giải ảnh đầu vào & \begin{tabular}[c]{@{}c@{}}1 ảnh global \(224\times224\) \\ và 4 tile cùng kích thước\end{tabular} \\ \hline
Bộ tối ưu hóa                & AdamW               \\ \hline
Tốc độ học                   & $5 \times 10^{-5}$  \\ \hline
Suy giảm trọng số            & $1 \times 10^{-4}$  \\ \hline
Bộ điều chỉnh tốc độ học     & Cosine              \\ \hline
Tỷ lệ khởi động              & 0.1                 \\ \hline
Kích thước lô                & 16                  \\ \hline
Số epoch tối đa              & 30                  \\ \hline
Số epoch dừng sớm            & 3 epochs            \\ \hline
Nhiệt độ tương phản ($\tau$) & 0.07                \\ \hline
Trạng thái tham số Backbone  & Tinh chỉnh qua LoRA \\ \hline
Hạng LoRA ($r$)             & 16                  \\ \hline
Hệ số LoRA ($\alpha$)       & 32                  \\ \hline
Tỷ lệ bỏ học LoRA           & 0.1                 \\ \hline
\end{tabular}
\caption{Bảng tổng hợp siêu tham số ở pha 1 và thiết lập của chúng}
\label{tab:hyperparameters_phase1}
\end{table}

\begin{table}[htbp]
\centering
\begin{tabular}{|l|c|}
\hline
\textbf{Siêu tham số}        & \textbf{Pha 2} \\ \hline
Độ phân giải ảnh đầu vào     & Giống pha 1                  \\ \hline
Bộ tối ưu hóa                & AdamW                        \\ \hline
Tốc độ học                   & $1 \times 10^{-4}$           \\ \hline
Suy giảm trọng số            & $1 \times 10^{-4}$           \\ \hline
Bộ điều chỉnh tốc độ học     & Cosine                       \\ \hline
Tỷ lệ khởi động              & 0.1                          \\ \hline
Kích thước lô                & 16                           \\ \hline
Số epoch tối đa              & 50                           \\ \hline
Số epoch dừng sớm            & 5 epochs                     \\ \hline
Nhiệt độ tương phản ($\tau$) & Không áp dụng                \\ \hline
Trạng thái tham số Backbone  & Tinh chỉnh qua LoRA          \\ \hline
Hạng LoRA ($r$)             & 16                           \\ \hline
Hệ số LoRA ($\alpha$)       & 32                           \\ \hline
Tỷ lệ bỏ học LoRA           & 0.1                          \\ \hline
\end{tabular}
\caption{Bảng tổng hợp siêu tham số ở pha 2 và thiết lập của chúng}
\label{tab:hyperparameters_phase2}
\end{table}

\section{Nội dung thực nghiệm}
Phần này trình bày các nhóm thí nghiệm được thực hiện trên hai bộ dữ liệu BTXRD và CTCH, bao gồm nội dung, mục đích và các mô hình sử dụng.

\subsection{Đánh giá mô hình nền tảng}
\label{subsec:foundation_model_evaluation}

\textbf{Thiết kế mô hình:} Nhóm thí nghiệm này sử dụng bốn mô hình ngôn ngữ
trực quan gồm CLIP, MedCLIP, PubMedCLIP và BiomedCLIP ở chế độ zero-shot. Toàn
bộ tham số của backbone được giữ cố định; mô hình không được huấn luyện trên dữ liệu đích. Với mỗi lớp $c$, tên lớp tiếng Anh được đưa vào một cặp prompt cố định gồm prompt khẳng định và prompt phủ định:
\begin{quote}
\texttt{this is an image of a bone x-ray; [class] presented in image},\\
\texttt{this is an image of a bone x-ray; no [class] presented in image}.
\end{quote}
Chẳng hạn, đối với lớp osteosarcoma, hai prompt lần lượt là
\begin{quote}
\texttt{this is an image of a bone x-ray; osteosarcoma presented in image}, \\
\texttt{this is an image of a bone x-ray; no osteosarcoma presented in image}.
\end{quote}
Hai prompt được mã hóa thành biểu diễn văn bản và so sánh với biểu diễn ảnh. Độ tương đồng của cặp khẳng định phủ định được chuyển thành xác suất bằng softmax với nhiệt độ $\tau$; xác suất của prompt khẳng định được dùng làm điểm cho lớp $c$. Nhãn dự đoán là lớp có điểm khẳng định lớn nhất trong toàn bộ tập lớp.

\textbf{Mục đích thực nghiệm:} Thí nghiệm khảo sát khả năng chuyển trực tiếp
tri thức đã tiền huấn luyện sang bài toán phân loại X-quang xương. Thiết lập này cung cấp một mốc tham chiếu không sử dụng mẫu huấn luyện của bộ dữ liệu đích, qua đó cho phép xem xét mức độ phù hợp ban đầu của các mô hình ngôn ngữ trực quan

\textbf{Dữ liệu và độ đo:} Bốn mô hình được đánh giá độc lập trên tập kiểm thử của BTXRD và CTCH; các tập huấn luyện và validation không được sử dụng để cập nhật mô hình. Các độ đo báo cáo gồm Accuracy, Balanced Accuracy, Macro-F1, Macro-AUROC, và Macro-AUPRC theo định nghĩa tại Mục \ref{sec:evaluation_metrics}.

\subsection{So sánh với các mô hình nền tảng}
\label{subsec:foundation_model_comparison}

\textbf{Thiết kế mô hình:} Nhóm đối sánh có ba thiết lập bổ sung cho nhau:
\begin{enumerate}
    \item \textbf{Mẫu hạn chế:} LoRA-BiomedCLIP, LoRA-PubMedCLIP và XBone-Net được huấn luyện với 1, 10 hoặc 20 mẫu cho mỗi lớp ở tập huấn luyện, tập đánh giá và kiểm thử được giữ nguyên. Hai mô hình áp dụng kỹ thuật LoRA sử dụng quy trình hai pha, phép nối đặc trưng và đầu phân loại tuyến tính.
    \item \textbf{Toàn bộ dữ liệu:} ResNet-50 và DenseNet-121 được tinh chỉnh toàn bộ với đầu tuyến tính và chỉ nhận ảnh. Các mô hình ngon ngữ trực quan y khoa gồm CLIP, MedCLIP, PubMedCLIP và BiomedCLIP được tinh chỉnh toàn bộ, sử dụng ảnh cùng báo cáo lâm sàng, phép nối đặc trưng và đầu tuyến tính.
    \item \textbf{Đối sánh tinh chỉnh hiệu quả tham số:} Hai mô hình BiomedCLIP và PubMedCLIP được áp dụng kỹ thuật LoRA. Hai mô hình này giữ phép nối đặc trưng và đầu tuyến tính.
\end{enumerate}

\textbf{Mục đích thực nghiệm:} Thiết lập mẫu hạn chế đánh giá khả năng học khi số mẫu gán nhãn bị giới hạn và mức suy giảm so với khi huấn luyện trên toàn bộ dữ liệu khi áp dụng kỹ thuật LoRA. Thiết lập toàn bộ dữ liệu cung cấp đối sánh tổng thể giữa mô hình ảnh đơn phương thức, các mô hình ngôn ngữ trực quan y khoa được tinh chỉnh và pipeline đề xuất. Đối sánh tinh chỉnh hiệu quả tham số được dùng để xem xét liệu các mô hình ngôn ngữ trực quan y khoa có thể đạt được hiệu suất tương tự như mô hình tinh chỉnh toàn bộ khi chỉ được huấn luyện với một số lượng nhỏ tham số.

\textbf{Dữ liệu và độ đo:} Tất cả ba thiết lập được thực hiện trên BTXRD và CTCH với các tập huấn luyện, validation và kiểm thử cố định. Kết quả được tổng hợp trên ba hạt giống 42, 123 và 456 bằng Accuracy, Balanced Accuracy, Macro-F1, Macro-AUROC, Macro-AUPRC và ECE. Khoảng tin cậy bootstrap 95\% với $B=10.000$ được tính trên tập kiểm thử. Nhằm mô tả đánh đổi giữa hiệu năng dự đoán và chi phí suy luận, tổng số và số tham số có thể huấn luyện, GFLOPs trên mỗi mẫu, thông lượng, độ trễ suy luận và bộ nhớ GPU cực đại cũng được báo cáo.

\subsection{OOD hậu xử lý}
\label{subsec:ood_postprocessing}

\textbf{Thiết kế phương pháp.} Phân tích OOD không huấn luyện lại mô hình phân loại mà sử dụng trọng số của CTCH ở ba hạt giống. Bốn score hậu xử lý gồm khoảng cách cosine tới tâm lớp gần nhất, khoảng cách Mahalanobis tới tâm lớp với hiệp phương sai Ledoit--Wolf, khoảng cách cosine kNN với $k=5$ và entropy dự đoán. Cả ba score hình học chỉ sử dụng fused embedding đã được huấn luyện, trong đó token ảnh toàn cục, các token ảnh cục bộ độ phân giải cao và các token bệnh sử được kết hợp bằng cross-attention hai chiều trước khi chiếu qua fusion MLP; entropy được tính từ phân phối xác suất của cùng checkpoint. Thống kê của bộ phát hiện đặc trưng được ước lượng từ CTCH train. Ngưỡng vận hành được hiệu chỉnh chỉ trên CTCH validation-ID sao cho tỷ lệ từ chối ID xấp xỉ 5\%, sau đó được khóa trước khi đánh giá.

\textbf{Mục đích thực nghiệm.} Nhóm thí nghiệm đánh giá liệu các điểm hậu xử lý có thể tách mẫu ngoài phân phối khỏi mẫu thuộc phân phối huấn luyện mà không thay đổi bộ phân loại hay không. Ba dạng sai lệch được xem xét riêng gồm khác biệt ngữ nghĩa bệnh, dịch chuyển miền ảnh và sự không nhất quán giữa ảnh với bệnh sử. Cách tách scenario này hạn chế việc quy mọi trường hợp bất thường về một nguồn sai lệch duy nhất.

\textbf{Dữ liệu và độ đo.} Tập ID gồm CTCH train để khớp bộ phát hiện, CTCH
validation để hiệu chỉnh ngưỡng và CTCH test để đánh giá. Semantic OOD sử dụng
archive \texttt{ctch\_ood}; domain OOD sử dụng riêng tập kiểm thử FracAtlas và
750 mẫu thuộc tập kiểm thử BTXRD. Với cả hai nguồn domain OOD, Mahalanobis và
kNN trên biểu diễn fused ảnh--bệnh sử là phân tích chính. BTXRD được xem như một nguồn
OOD khác bộ dữ liệu, kết hợp dịch chuyển miền thu nhận với thay đổi không gian
nhãn theo các bệnh lý u xương; riêng lớp bình thường có ngữ nghĩa giao một phần
với CTCH. Vì bệnh sử \texttt{clinical\_v2} của BTXRD là văn bản tổng hợp, kết quả
trên nguồn này được diễn giải như khả năng phát hiện OOD đa phương thức, không
được dùng làm bằng chứng riêng cho dịch chuyển miền ảnh. Report mismatch được tạo bằng
cách ghép ảnh với bệnh sử khác lớp, còn ghép cùng lớp là phân tích phụ. Ba độ đo
chính là AUROC-OOD, AUPR-Out và FPR@95\%TPR với OOD là lớp dương; khoảng tin cậy
bootstrap 95\% được tính với $B=2.000$ lần lấy mẫu lại. AUROC-OOD và AUPR-Out
cao hơn biểu thị khả năng phân tách hoặc chất lượng cảnh báo tốt hơn, trong khi
FPR@95\%TPR thấp hơn biểu thị ít mẫu ID bị cảnh báo nhầm hơn tại mức phát hiện
95\% OOD.

\subsection{Giải thích và biểu diễn}
\label{subsec:explainability_representation}

\textbf{Thiết kế phương pháp.} Phân tích sử dụng checkpoint XBone-Net canonical
trên CTCH ở ba hạt giống. Với mỗi hạt giống, mẫu test được lấy phân tầng theo nhãn thật,
tối đa hai mẫu mỗi lớp và không quá 44 mẫu. Integrated Gradients với 24 bước
được áp dụng cho các token ảnh cục bộ; token bệnh sử được phân tích bằng can
thiệp thay thế. Độ trung thực được kiểm tra bằng các đường deletion và insertion
so với thứ tự ngẫu nhiên và thứ tự ít liên quan. Độ ổn định được khảo sát dưới
nhiễu nhỏ, đồng thời classifier-randomization được dùng như một sanity check.
Phần biểu diễn khảo sát sáu không gian gồm đặc trưng fused, ảnh toàn cục, tóm tắt
ảnh cục bộ, văn bản toàn cục và hai hướng cross-attention.

\textbf{Mục đích thực nghiệm.} Phân tích attribution nhằm xác định mức độ phụ
thuộc của dự đoán vào các token ảnh và bệnh sử, đồng thời kiểm tra liệu thứ hạng
attribution có ổn định và phản ánh đầu ra của mô hình hay không. Phân tích biểu
diễn khảo sát mức độ kết cụm theo lớp và khoảng cách giữa mẫu với tâm lớp trong
từng không gian nhúng. CTCH chưa có annotation vùng tổn thương trong protocol,
do đó nhóm này không báo cáo IoU, Dice hoặc độ chính xác định vị.

\textbf{Dữ liệu và độ đo.} Phân tích được thực hiện trên tập con test CTCH đã
chọn và báo cáo Attention Entropy riêng cho hai hướng chú ý, Fusion Contribution
của từng hướng, Attention Context Cosine giữa hai vector ngữ cảnh và Prediction
Drop after Intervention khi lần lượt loại bỏ từng nhánh dung hợp. Các phân tích
hình học biểu diễn khác được trình bày như kết quả bổ sung và không thuộc bộ độ
đo chính được định nghĩa tại Mục \ref{sec:evaluation_metrics}. Các đại lượng chú
ý và dung hợp được xem là chỉ báo định lượng bổ trợ cho phân tích cơ chế, không
phải bằng chứng trực tiếp về khả năng định vị tổn thương hoặc quan hệ nhân quả
lâm sàng.

\subsection{Đánh giá thành phần}
\label{subsec:component_evaluation}

\textbf{Thiết kế biến thể.} Các biến thể kế thừa cấu hình của mô hình đề xuất ngoài thành phần đang khảo sát, dữ liệu, hạt giống và quy trình chọn checkpoint được giữ cố định. Bốn nhóm biến thể gồm:
\begin{enumerate}
    \item \textbf{Tiền xử lý ảnh:} mô hình đề xuất dùng một ảnh toàn cục và
    bốn vùng tiêu điểm thưa. Biến thể \textbf{nohighres} dùng direct resize về $224\times224$, còn \textbf{letterbox} giữ tỷ lệ ảnh và thêm biên đệm. Hai biến thể vẫn tạo tám token từ một ảnh để giữ ổn định đầu vào của mô-đun dung hợp.
    \item \textbf{Tinh chỉnh:} mô hình đề xuất cập nhật adapter
    LoRA trong cả hai pha huấn luyện. Biến thể \textbf{no finetune} đóng băng các encoder được kế thừa từ BioMedCLIP \cite{BiomedCLIP} nhưng vẫn cho phép mô-đun lấy mẫu không gian được huấn luyện và \textbf{full finetune} cập nhật toàn bộ tham số mô hình kế thừa.
    \item \textbf{Dung hợp:} cross-attention hai chiều của mô hình đề xuất
    được so sánh với phép nối đặc trưng và hai biến thể attention
    \textbf{một chiều ảnh sang văn bản}, \textbf{văn bản sang ảnh}.
    \item \textbf{Đầu phân loại và cân bằng lớp:} đầu tính tâm cụm với
    trọng số lớp được so sánh với biến thể sử dụng đầu tuyến tính và biến thể giữ đầu tính tâm cụm nhưng loại bỏ trọng số lớp.
\end{enumerate}

\textbf{Mục đích thực nghiệm.} Nhóm thí nghiệm lần lượt xem xét vai trò của việc bảo toàn chi tiết ảnh độ phân giải cao, chiến lược cập nhật mô hình kế thừa, hướng trao đổi thông tin giữa hai phương thức và lựa chọn đầu phân loại trong điều kiện mất cân bằng lớp. Thiết kế kế thừa cùng cấu hình giúp giới hạn thay đổi trong từng phép so sánh, từ đó hỗ trợ diễn giải chênh lệch theo thành phần.

\textbf{Dữ liệu và độ đo.} Toàn bộ đánh giá thành phần được thực hiện trên CTCH với ba hạt giống 42, 123 và 456. Hiệu năng phân loại được đánh giá bằng Accuracy, Balanced Accuracy, Macro-F1, Macro-AUROC, Macro-AUPRC và ECE, kèm Confusion Matrix cho các biến thể cần phân tích sai số theo lớp và khoảng tin cậy bootstrap 95\% với $B=10.000$. Với nhóm tinh chỉnh và tiền xử lý ảnh, tổng số và số tham số có thể huấn luyện, GFLOPs trên mỗi mẫu, thông lượng, độ trễ suy luận và bộ nhớ GPU cực đại được ghi nhận bổ sung để mô tả đánh đổi về tài nguyên.

\section{Kết quả thực nghiệm và thảo luận}
\label{sec:ch4draft_results}

\subsection{Đánh giá mô hình nền tảng}

\input{generated/chapter4_draft/table_zero_shot}

BiomedCLIP đạt giá trị cao nhất trên cả năm độ đo của BTXRD và bốn trong năm độ đo của CTCH; riêng Balanced Accuracy trên CTCH, CLIP đạt $0{.}2367$, cao hơn $0{.}2109$ của BiomedCLIP. Tuy nhiên, Macro-F1 tốt nhất chỉ đạt $0{.}1289$ trên BTXRD và $0{.}1659$ trên CTCH. Vì XBone-Net kế thừa backbone BiomedCLIP, kết quả này cho thấy điểm khởi đầu của mô hình đề xuất tốt hơn phần lớn backbone còn lại, đồng thời cũng cho thấy việc dùng trực tiếp mô hình nền tảng chưa đủ để giải quyết quyết định đa lớp.

Khoảng cách giữa Macro-AUROC và Macro-F1 có thể xuất phát từ sự không tương thích giữa prompt chung, tên lớp và miền ảnh X-quang xương, kết hợp với mất cân bằng lớp và khác biệt phân bố so với dữ liệu tiền huấn luyện. BiomedCLIP có lợi thế nhờ tri thức y sinh và liên kết ảnh--văn bản phù hợp hơn CLIP tự nhiên; ngược lại, MedCLIP có kết quả thấp trong protocol hiện tại, cho thấy lợi thế miền y khoa không tự động chuyển thành khả năng phân biệt các lớp giải phẫu và bệnh lý cụ thể.

Kết quả zero-shot cung cấp hai insight chính: BiomedCLIP là lựa chọn backbone hợp lý cho XBone-Net, nhưng phần đóng góp cần được định vị ở quá trình thích ứng, xử lý ảnh đa tỉ lệ, dung hợp bệnh sử và thiết kế đầu ra thay vì ở bản thân mô hình nền tảng. Các kết quả zero-shot vì vậy được sử dụng như mốc khởi đầu, không phải đối chứng trực tiếp cho hiệu quả của toàn bộ pipeline sau huấn luyện.

\subsection{So sánh với các mô hình nền tảng}

\input{generated/chapter4_draft/table_full_shot}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/chapter4_draft/main/fullshot_comparison.png}
\caption{So sánh XBone-Net với hai baseline LoRA gần nhất trong thiết lập full-shot}
\label{fig:ch4draft_fullshot}
\end{figure}

\input{generated/chapter4_draft/table_few_shot}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/chapter4_draft/main/fewshot_macro_f1.png}
\caption{Macro-F1 trong các thiết lập mẫu hạn chế; thanh sai số biểu thị độ lệch chuẩn giữa ba hạt giống}
\label{fig:ch4draft_fewshot}
\end{figure}

\input{generated/chapter4_draft/table_efficiency}

\begin{figure}[H]
\centering
\includegraphics[width=0.78\textwidth]{images/chapter4_draft/main/efficiency_pareto.png}
\caption{Đánh đổi giữa Macro-F1 trung bình ba hạt giống và độ trễ forward pass đo tại seed 42 trên CTCH}
\label{fig:ch4draft_efficiency_pareto}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{images/chapter4_draft/main/ctch_confusion_matrix.png}
\caption{Ma trận nhầm lẫn chuẩn hóa theo nhãn thực của XBone-Net trên CTCH, gộp ba hạt giống}
\label{fig:ch4draft_confusion}
\end{figure}

Trong full-shot, XBone-Net đạt Macro-AUROC cao nhất trên cả BTXRD ($0{.}9243\pm0{.}0298$) và CTCH ($0{.}9474\pm0{.}0246$), đồng thời Macro-AUPRC trên CTCH ($0{.}6146\pm0{.}0132$) gần với BiomedCLIP tốt nhất ($0{.}6169\pm0{.}0090$). Điểm yếu nằm ở quyết định cứng: trên BTXRD, Macro-F1 $0{.}6010$ thấp hơn LoRA-PubMedCLIP $0{.}6327$; trên CTCH, Accuracy $0{.}5655$ thấp hơn $0{.}6009$ của LoRA-PubMedCLIP và Macro-F1 thấp hơn nhẹ CLIP. Trong few-shot, XBone-Net tốt nhất ở toàn bộ năm độ đo tại CTCH 1-shot và dẫn đầu Macro-F1 tại BTXRD 10-shot, 20-shot cùng CTCH 10-shot, nhưng không duy trì vị trí dẫn đầu trên mọi độ đo ở CTCH 20-shot. Chi phí của các mô-đun bổ sung cũng cao hơn: 229,02 GFLOPs, latency 87,15 ms và throughput 13,4 mẫu/giây, so với 83,32 GFLOPs và 38,3 mẫu/giây của LoRA-BiomedCLIP.

Macro-AUROC cao nhưng Macro-F1 không cao nhất cho thấy fused embedding duy trì thứ tự tương đối giữa lớp dương và lớp âm tốt hơn khả năng đặt ranh giới quyết định cuối cùng. Cross-attention và nhánh ảnh đa tỉ lệ bổ sung thông tin giúp xếp hạng, đặc biệt khi dữ liệu rất ít; tuy nhiên, empirical centroid với class weight và class-specific bias có thể làm dịch chuyển quyết định argmax, nhất là khi tâm lớp được ước lượng từ ít mẫu hoặc các lớp chồng lấn. Ma trận nhầm lẫn củng cố nhận định này: một số lớp có recall cao như đùi và gót, trong khi trụ, cột sống ngực, cắt cụt ngón tay và thuyền còn thấp. So với baseline LoRA dùng concat và đầu tuyến tính, XBone-Net đổi thêm chi phí tính toán để mô hình hóa tương tác token cục bộ--bệnh sử; lợi ích này rõ hơn ở xếp hạng xác suất và regime ít mẫu so với Accuracy tổng thể.

Insight rút ra là đóng góp phân loại phù hợp nhất của XBone-Net không phải sự vượt trội đồng đều trên mọi độ đo, mà là khả năng duy trì Macro-AUROC tốt trên hai miền dữ liệu và cải thiện khả năng học ở một số thiết lập few-shot. Điểm cần cải thiện nằm ở bước chuyển từ biểu diễn sang quyết định lớp và ở chi phí của nhánh ảnh độ phân giải cao. Vì vậy, kết luận của tiểu mục nên tách rõ chất lượng biểu diễn/xếp hạng khỏi chất lượng quyết định tại một ngưỡng, đồng thời không diễn giải latency forward-only như thời gian của toàn bộ quy trình lâm sàng.

\subsection{Đánh giá thành phần}

\input{generated/chapter4_draft/table_ablation}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/chapter4_draft/main/ablation_macro_f1_delta.png}
\caption{Chênh lệch Macro-F1 của từng biến thể so với cấu hình XBone-Net chuẩn trên CTCH}
\label{fig:ch4draft_ablation}
\end{figure}

So với XBone-Net chuẩn, việc chỉ dùng ảnh làm giảm Macro-F1 khoảng 0,159, chỉ dùng văn bản giảm khoảng 0,080 và xáo trộn bệnh sử giảm khoảng 0,254; đây là các suy giảm lớn nhất trong bảng. Các hướng cross-attention một chiều có Macro-F1 gần cấu hình hai chiều, trong khi đầu tuyến tính đạt $0{.}5889\pm0{.}0251$, cao hơn XBone-Net chuẩn $0{.}5574\pm0{.}0049$. Ngược lại, XBone-Net chuẩn có Macro-AUROC $0{.}9474$, cao hơn đầu tuyến tính $0{.}9080$, nhưng thấp hơn nhẹ biến thể concat $0{.}9566$ và biến thể không class weight $0{.}9528$. Vì vậy, không có một cấu hình duy nhất tốt nhất trên mọi khía cạnh.

Suy giảm của image-only, text-only và shuffled-report cho thấy hai phương thức có thông tin bổ sung và quan hệ ghép cặp đúng là thành phần có ảnh hưởng rõ nhất. Khoảng cách nhỏ giữa dung hợp hai chiều và một chiều gợi ý rằng cross-attention hiện tại chưa khai thác đầy đủ cả hai hướng; attention phân tán và trùng lặp thông tin giữa các token có thể làm giảm phần lợi ích bổ sung. Việc đầu tuyến tính tăng Macro-F1 nhưng làm giảm Macro-AUROC cho thấy empirical centroid không phải lựa chọn tối ưu nếu mục tiêu duy nhất là quyết định argmax, song hình học theo tâm lớp vẫn hỗ trợ xếp hạng, phân tích biểu diễn và OOD. Class weight và class-specific bias cũng tạo đánh đổi giữa BAcc, F1 và AUROC thay vì cải thiện đồng thời.

Insight quan trọng nhất là đóng góp được hậu thuẫn mạnh hiện tại nằm ở tính đa phương thức và sự nhất quán ảnh--bệnh sử, trong khi lợi ích riêng của cross-attention hai chiều và empirical centroid cần được diễn giải có điều kiện theo độ đo. Một hướng thực tế là giữ fused representation để phục vụ xếp hạng và OOD nhưng hiệu chỉnh hoặc thay đầu quyết định khi ưu tiên Macro-F1. Các biến thể tiền xử lý bị suy biến phải được chạy lại trước khi kết luận về attention pooling hoặc ảnh độ phân giải cao; việc loại chúng khỏi bảng hiện tại giúp tránh đưa ra kết luận từ kết quả không hợp lệ.

\subsection{OOD hậu xử lý}

\input{generated/chapter4_draft/table_ood}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/chapter4_draft/main/ood_metric_overview.png}
\caption{Tổng quan ba độ đo OOD trên năm kịch bản; mọi score chỉ sử dụng fused embedding hoặc entropy đầu ra của cùng checkpoint XBone-Net}
\label{fig:ch4draft_ood_overview}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{images/chapter4_draft/main/ood_fracatlas_distribution.png}
\caption{Phân phối OOD score của CTCH-ID và FracAtlas tại seed 42; đường đứt nét là ngưỡng khóa từ validation-ID}
\label{fig:ch4draft_ood_fracatlas}
\end{figure}

Trên cùng biểu diễn XBone-Net, Mahalanobis-centroid là score hậu xử lý tốt nhất cho domain OOD: AUROC-OOD đạt $0{.}8851$ trên FracAtlas và $0{.}8077$ trên BTXRD, đồng thời có FPR@95\%TPR thấp nhất tương ứng $0{.}4763$ và $0{.}6764$. kNN tốt nhất cho semantic OOD và mismatch khác lớp với AUROC-OOD lần lượt $0{.}6279$ và $0{.}7616$. Điểm yếu chung xuất hiện ở mismatch cùng lớp, nơi AUROC-OOD tốt nhất chỉ đạt $0{.}5745$ và FPR@95\%TPR vẫn lớn hơn $0{.}92$. Entropy nhìn chung yếu hơn các score hình học, đặc biệt trên domain OOD. Đây là so sánh giữa các baseline score hậu xử lý của cùng XBone-Net, chưa phải bằng chứng rằng mô hình OOD tốt hơn một kiến trúc ngoại lai.

Mahalanobis-centroid tận dụng độ lệch theo phương sai và tương quan giữa các chiều fused embedding, do đó phù hợp khi FracAtlas hoặc BTXRD tạo ra dịch chuyển miền tương đối có cấu trúc. kNN chỉ dựa trên lân cận cục bộ nên nhạy hơn với semantic OOD và cặp ảnh--bệnh sử khác lớp nằm xa các mẫu huấn luyện gần nhất. Mismatch cùng lớp khó hơn vì ảnh và bệnh sử thay thế vẫn mang tín hiệu lớp tương đồng; fused embedding có thể giữ nhãn ngữ nghĩa trong khi không mã hóa đủ định danh cá thể để phát hiện sự không nhất quán. AUPR-Out thấp trong semantic OOD còn chịu ảnh hưởng của tỷ lệ OOD thấp, vì vậy phải đọc cùng AUROC-OOD và FPR@95\%TPR thay vì tách riêng.

Insight của thí nghiệm là fused embedding chứa tín hiệu hữu ích cho domain OOD và mismatch khác lớp mà không cần huấn luyện thêm, nhưng một score duy nhất không phù hợp cho mọi loại dịch chuyển. Mahalanobis-centroid là lựa chọn chính cho domain OOD, còn kNN phù hợp hơn với bất thường ngữ nghĩa và mismatch khác lớp. Khả năng phát hiện mismatch cùng lớp hiện chưa đủ mạnh để đề xuất như chức năng tin cậy; kết quả này nên được báo cáo như một giới hạn và là cơ sở cho nghiên cứu representation riêng cho tính nhất quán liên phương thức.

\subsection{Giải thích và biểu diễn}

\input{generated/explainability/table_explanation_mechanism}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{generated/explainability/explanation_mechanism_summary.png}
\caption{Phân tích cơ chế chú ý và dung hợp của XBone-Net trên CTCH: (a) phân phối Attention Entropy chuẩn hóa; (b) phân phối Attention Context Cosine, trong đó đường đứt nét biểu thị giá trị trung bình; (c) đóng góp tương đối của hai nhánh; và (d) mức giảm xác suất lớp mục tiêu sau can thiệp. Hai phân phối được gộp từ các mẫu kiểm thử của ba seed, trong khi thanh sai số ở (c) và (d) biểu thị độ lệch chuẩn giữa ba seed}
\label{fig:explainability_mechanism_summary}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{generated/explainability/attention_intervention_curves.png}
\caption{Đường cong can thiệp token ảnh và token bệnh sử theo ba chiến lược: thay thế token quan trọng nhất, thay thế ngẫu nhiên và thay thế token ít quan trọng nhất; đường liền biểu thị giá trị trung bình và vùng màu biểu thị độ lệch chuẩn giữa ba seed}
\label{fig:ch4draft_attention_intervention}
\end{figure}

\input{generated/explainability/table_representation_geometry}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{generated/explainability/representation_geometry_summary.png}
\caption{So sánh hình học của các không gian biểu diễn trong XBone-Net trên CTCH; điểm và thanh sai số lần lượt biểu thị trung bình và độ lệch chuẩn giữa ba seed}
\label{fig:representation_geometry_summary}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{generated/explainability/attention_cases.png}
\caption{Hai trường hợp định tính tại seed 42: một dự đoán đúng và một dự đoán sai có độ tin cậy cao}
\label{fig:ch4draft_attention_cases}
\end{figure}

So với các nhánh riêng, fused embedding có hình học tốt nhất trên cả bốn độ đo: Silhouette ít âm hơn, Davies--Bouldin thấp hơn, NMI đạt $0{.}5470$ và centroid BAcc đạt $0{.}5899$. Nhánh image-from-text đóng góp khoảng $0{.}7031$ và gây prediction drop $0{.}3461$ khi bị loại, cao hơn nhánh text-from-image tương ứng $0{.}2969$ và $0{.}1072$. Tuy nhiên, attention entropy đều cao ($0{.}9417$ và $0{.}9653$), context cosine gần 0, Silhouette của fused embedding vẫn âm và targeted deletion chỉ tạo chênh lệch xác suất nhỏ so với random deletion. XBone-Net vì vậy có bằng chứng về việc dùng cả hai phương thức, nhưng chưa có bằng chứng mạnh rằng attention tập trung ổn định vào một tập token hẹp hoặc tạo cụm lớp tách biệt hoàn toàn.

Fused embedding cải thiện so với từng nhánh vì cross-attention và fusion MLP kết hợp tín hiệu giải phẫu, vùng cục bộ và bệnh sử trước khi tạo biểu diễn quyết định. Đóng góp lớn hơn của image-from-text phù hợp với vai trò bệnh sử trong việc điều kiện hóa nhánh ảnh; ngược lại, context cosine gần 0 cho thấy hai hướng attention bổ sung các ngữ cảnh khác nhau thay vì học cùng một tín hiệu. Attention entropy cao có thể xuất phát từ nhiều token liên quan đồng thời hoặc attention chưa đủ chọn lọc. Trường hợp dự đoán sai có độ tin cậy cao và đường can thiệp gần phẳng cho thấy heatmap trực quan không tự thân bảo đảm tính trung thực nhân quả, đặc biệt khi không có ground truth vùng tổn thương.

Insight kết luận là lợi ích rõ nhất của cơ chế dung hợp nằm ở chất lượng tương đối của fused representation và sự phụ thuộc dự đoán vào ngữ cảnh image-from-text, không phải ở khả năng định vị tổn thương đã được xác nhận. Các hình attention nên được dùng để minh họa giả thuyết hoạt động và đi kèm can thiệp token, đồng thời tránh diễn giải như lời giải thích lâm sàng hoàn chỉnh. Kết quả hiện tại cũng gợi ý hướng cải thiện bằng regularization attention, kiểm tra tính ổn định giữa seed và bổ sung annotation vùng tổn thương; cho đến khi có các kiểm chứng đó, chức năng giải thích nên được xem là phân tích hỗ trợ.

\section{Tóm tắt chương}

Kết quả hiện có cho thấy XBone-Net nổi bật nhất ở Macro-AUROC trên hai bộ dữ liệu, một số thiết lập few-shot, fused representation và khả năng dùng score hình học để phát hiện domain OOD. Mô hình chưa vượt trội đồng đều về Accuracy, Balanced Accuracy hoặc Macro-F1; đầu tuyến tính còn đạt Macro-F1 cao hơn trong ablation, chi phí suy luận lớn hơn các baseline LoRA và khả năng phát hiện mismatch cùng lớp còn yếu. Vì vậy, đóng góp nên được trình bày như một pipeline phân tích ảnh X-quang xương đa phương thức có chất lượng xếp hạng và khả năng phân tích hậu xử lý, thay vì khẳng định ưu thế tuyệt đối về phân loại quyết định.
