# @package _global_
# Baseline B4: BiomedCLIP ViT-B/16 on BTXRD
defaults:
  - /model/peft@model.peft: qlora
  - /model/fusion@model.fusion: none
  - /model/classifier@model.classifier: none
  - /dataset@dataset: btxrd
  - _self_

model:
  backbone_type: "biomedclip"
  freeze_backbone: false
  _placeholder_: true
dataset:
  _placeholder_: true

experiment_name: "btxrd/baselines/finetuned/biomedclip"

params:
  run_phase1: true
  run_phase2: true
  model_dir: "checkpoints/${experiment_name}/seed_${seed}/"
  phase1:
    epochs: 50
    lr: 2e-4
    weight_decay: 1e-2
    optimizer: "adamw"
    scheduler: "cosine"
    warmup_ratio: 0.1
    checkpoint_path: "checkpoints/${experiment_name}/seed_${seed}/best_phase1.pth"
    temperature: 0.07
    loss_type: "semantic_matching"
    early_stopping_patience: 5
    p1_report_type: "clinical"
  phase2:
    epochs: 50
    lr: 1e-3
    weight_decay: 1e-4
    optimizer: "adamw"
    scheduler: "cosine"
    warmup_ratio: 0.1
    early_stopping_patience: 5
    fusion_type: "cross_attention"
    classifier_type: "linear"
    use_text: true
    p2_report_type: "clinical"
    checkpoint_path: "checkpoints/${experiment_name}/seed_${seed}/best_phase2.pth"

seed: 42
