# @package _global_
# Modality robustness: reuse the canonical checkpoint and mismatch synthetic
# clinical text across different classes on the BTXRD test split only.

defaults:
  - /experiment/btxrd/proposed/ours_xbone_net
  - _self_

experiment_name: btxrd/ablation_study/modality/shuffled_report

dataset:
  params:
    shuffle_report_mode: cross_class
    shuffle_report_splits:
      - test
