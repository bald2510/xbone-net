# @package _global_
# Fine-tuning ablation: freeze BiomedCLIP while retaining a trainable spatial
# resampler. "no_ft" refers only to the foundation visual/text encoders.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net_v3
  - _self_

experiment_name: ctch/ablation_study/finetune/xbone_highres_no_ft

model:
  freeze_backbone: true
  peft:
    type: none
    params: null
