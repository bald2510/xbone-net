# @package _global_
# Fusion ablation: global image queries local text. The text branch retains
# only its normalized global embedding before concatenation and the same MLP.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net
  - _self_

experiment_name: ctch/ablation_study/fusion/image_to_text

params:
  phase2:
    fusion_params:
      attention_direction: image_to_text
