# @package _global_
# Fusion ablation: global text queries local image. The image branch retains
# only its normalized global embedding before concatenation and the same MLP.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net
  - _self_

experiment_name: ctch/ablation_study/fusion/text_to_image

params:
  phase2:
    fusion_params:
      attention_direction: text_to_image
