# @package _global_
# Local-resolution ablation: reduce the v3 local-token budget from 20 to 8.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net
  - _self_

experiment_name: ctch/ablation_study/architecture/preprocess/xbone_reduced_local_tokens

model:
  num_visual_tokens: 8
  local_pool_grid: 1
