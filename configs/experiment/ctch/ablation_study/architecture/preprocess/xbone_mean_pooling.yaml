# @package _global_
# High-resolution aggregation ablation: replace v3 attention pooling with mean pooling.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net_v3
  - _self_

experiment_name: ctch/ablation_study/architecture/preprocess/xbone_mean_pooling

model:
  contrastive_pooling: mean
