# @package _global_
# Modality robustness: reuse the canonical checkpoint and mismatch reports only
# at test time. tools/training.py resolves the checkpoint source explicitly.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net
  - _self_

experiment_name: ctch/ablation_study/modality/shuffled_report

dataset:
  params:
    shuffle_report_mode: cross_class
    shuffle_report_splits:
      - test
