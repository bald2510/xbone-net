# @package _global_
# Input/modality ablation: shuffle reports in every split. This measures
# whether training can learn to ignore mismatched text.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net
  - _self_

experiment_name: ctch/ablation_study/input/modality/shuffled_report_train_eval

dataset:
  params:
    shuffle_reports: true
