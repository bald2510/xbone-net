# @package _global_
# Add prototype regularization to the learnable cosine head. Compare this with
# learnable_cosine.yaml, not directly with the proposed model, to isolate loss.

defaults:
  - /experiment/ctch/ablation_study/classifier/learnable_cosine
  - _self_

experiment_name: ctch/ablation_study/classifier/learnable_cosine_proto_loss

params:
  phase2:
    loss:
      lambda_proto: 0.05
