# @package _global_
# Backbone/PEFT ablation: replace LoRA with full-backbone fine-tuning.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net
  - _self_

experiment_name: ctch/ablation_study/backbone/full_finetuning

model:
  freeze_backbone: false
  peft:
    type: full_ft
    params: {}
