# @package _global_
# Backbone/PEFT ablation: remove LoRA only while retaining both phases.

defaults:
  - /experiment/ctch/proposed/ours_xbone_net
  - _self_

experiment_name: ctch/ablation_study/backbone/frozen_backbone

model:
  freeze_backbone: true
  peft:
    type: none
    params: {}
