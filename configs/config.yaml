# Root Hydra configuration — experiment configs override options via +experiment=<name>
defaults:
  - _self_

experiment_name: "default"

params:
  run_phase1: true
  run_phase2: true
  gradient_checkpointing: true
  gradient_accumulation_steps: 1

seed: 42
