# Locked post-hoc OOD protocol for the canonical CTCH proposed model.
source_experiment: "ctch/proposed/ours_xbone_net"
seeds: [42, 123, 456]

fit_split: "ctch_train"
calibration_split: "ctch_val"
id_test_split: "ctch_test"
target_id_fpr: 0.05
knn_k: 5
temperature: 1.0
methods: [mahalanobis, knn, msp, entropy, energy, max_logit]
n_bootstrap: 2000
bootstrap_seed: 3107

mahalanobis:
  formulation: "classical_distance"
  class_centers: "empirical_train_means"
  covariance: "shared_ledoit_wolf"
  class_reduction: "minimum"
  detector_l2_normalization: false

scenarios:
  semantic_ood:
    archive: "ctch_ood"
    feature_key: "fused_embeddings"
    primary_methods: [mahalanobis, knn, msp, entropy, energy, max_logit]
    primary: true
  domain_ood:
    archive: "fracatlas_test"
    feature_key: "fused_embeddings"
    primary_methods: [mahalanobis, knn]
    primary: true
    rationale: "Mahalanobis and kNN operate on the trained bidirectional fused representation, which combines the global image token, high-resolution local image tokens, and clinical-text tokens."
  domain_ood_btxrd:
    archive: "btxrd_test"
    feature_key: "fused_embeddings"
    primary_methods: [mahalanobis, knn]
    primary: true
    rationale: "BTXRD is evaluated as multimodal cross-dataset OOD with Mahalanobis and kNN on the trained bidirectional fused representation. Results must be interpreted with the synthetic clinical_v2 report schema in mind."
  report_mismatch_cross_class:
    archive: "report_mismatch_cross_class"
    feature_key: "fused_embeddings"
    primary_methods: [mahalanobis, knn, msp, entropy, energy, max_logit]
    paired_by_image_id: true
    primary: true
  report_mismatch_same_class:
    archive: "report_mismatch_same_class"
    feature_key: "fused_embeddings"
    primary_methods: [mahalanobis, knn, msp, entropy, energy, max_logit]
    paired_by_image_id: true
    primary: false
